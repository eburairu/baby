
## 2024-04-20 - Fix Memory Exhaustion DoS in File Uploads
**脆弱性:** ファイルのアップロード時に `await file.read()` を使用して巨大なファイル全体を一括でメモリに読み込んでしまうメモリ枯渇（DoS）リスク。
**学び:** `MAX_FILE_SIZE`の検証が読み込み後に行われていたため、巨大なファイルによるクラッシュを防ぐことができなかった。
**予防:** ファイルをメモリに読み込む際は、1MBなどのチャンク単位で読み込み、上限サイズを超過した時点で即座に例外をスローしてプロセスを中断する。
