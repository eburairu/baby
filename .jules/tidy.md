2026-02-23 - [抽出] 授乳タイマーロジックのカスタムフック化

学び:
- `FeedingForm` コンポーネント内に複雑なタイマーロジック（`setInterval`, `useRef`, 状態管理）が混在して おり、可読性が低下していた。
- `useFeedingTimer` フックにロジックを抽出することで、コンポーネントはUIとフォームの状態管理に集中できるようになった。
- フックが純粋な状態と操作を返し、フォームとの同期はコンポーネント側の `useEffect` で行うことで、フック の再利用性を高め、依存関係を疎に保つことができた。

アクション:
- 他のタイマー機能や複雑な状態管理を持つコンポーネント（例: 陣痛タイマーなど）でも同様のパターンを適用できるか検討する。
- フォームの値と外部の状態（タイマーなど）を同期させる際は、方向（State -> Form か Form -> State か、あ るいは双方向か）を意識し、ループが発生しないように注意する。

2026-02-23 - [共通化] Dashboard Widgetのメモ化と比較ロジックの共通化

学び:
- 複数のダッシュボードウィジェットで `React.memo` を使用した再レンダリング制御ロジックが重複していた。
- 共通のProps型 (`BaseWidgetProps`)、メモ化ロジック ファクトリ (`createWidgetMemoComparison`)、およびロ ーディングUI (`WidgetLoading`) を導入することで、各 ウィジェットの実装を大幅に簡素化した。
- 高階コンポーネント (HOC) を強制的に適用するよりも 、ヘルパー関数として比較ロジックを提供する方が、コンポーネントの柔軟性を保つつ重複を排除できる。

アクション:
- 他の類似構造を持つUIコンポーネント（例えば設定画面の各セクションなど）でも、Propsの共通化とロジックの 抽出を検討する。

2026-02-23 - [共通化] APIクライアントの重複コードの リファクタリング

学び:
- `frontend/lib/api.ts` の各HTTPメソッド（`api.post`, `api.put` など）で `fetch` 呼び出しやエラーハンド リングのコードが重複しており、冗長だった。
- 共通の `request` ヘルパー関数にロジックを集約する ことで、コードベースを削減し、メンテナンス性を向上させた。
- Node.js 環境（テスト環境）での `Headers` オブジェ クトの挙動（キーが小文字に正規化される）により、テストの期待値が失敗した。実装を無理に合わせるのではなく、テストコードを正しい挙動に合わせて修正することが重要だった。

アクション:
- 他のユーティリティ関数（例えばデータ変換ロジックなど）でも、同様の重複パターンがないか確認し、共通関数への抽出を進める。
2025-05-24 - [Refactor] GrowthRecordFormの標準化
学び: GrowthRecordForm.tsx は shadcn/ui の Form コンポーネントを使用せず、独自の実装を行っていたため、コードベース全体の一貫性が損なわれていた。
また、useEffect による手動のリセットロジックは React Hook Form の values オプションを使用することで簡素 化できる。
アクション: GrowthRecordForm.tsx を shadcn/ui の Form コンポーネントを使用するようにリファクタリングし、React Hook Form のベストプラクティスに従う。これにより、コードの可読性が向上し、ボイラープレートコードが削減される。

2026-02-23 - [Refactor] BabyFormのshadcn/ui化と非推 奨パターンの排除

学び:
- `BabyForm` コンポーネントが標準的な `shadcn/ui` の `Form` コンポーネントを使用しておらず、一貫性が欠けていた。
- `useEffect` を使用したフォーム値のリセットは、`react-hook-form` の `values` オプションを使用することで宣言的に記述でき、複雑さを排除できる。
- `Controller` パターン (`FormField`) を使用することで、`RadioGroup` などの制御コンポーネントも統一的に 扱える。

アクション:
- 他のフォームコンポーネントでも同様の `useEffect`  によるリセットパターンがないか確認し、あれば `values` オプションへの置き換えを推奨する。

2026-02-23 - [共通化] 統計情報の表示ブロック (StatsBlock) の共通化

学び:
- 授乳、睡眠、排泄など、複数の機能で「アイコン付きの統計カード」というUIパターンが繰り返されていた。
- `StatsBlock` コンポーネントを作成し、カラーバリエ ーションを持たせることで、各機能のテーマカラーを維持しつつ実装を共通化できた。
- 完全に同一の構造ではなくても、`children` を活用し て可変部分（サブテキストや追加情報）を受け入れることで、柔軟な共通コンポーネントを作成できる。

アクション:
- Dashboard Widgetの内部構造にも似たパターンがある可能性があるため、将来的に `StatsBlock` または類似のコンポーネントを適用できるか検討する。

2026-02-23 - [共通化] useInterval フックと秒単位の時間フォーマット関数の導入

学び:
- `ContractionTimer`, `SleepTimer`, `useFeedingTimer` など、複数の場所で `setInterval` と `clearInterval` の手動管理が重複して行われていた。
- `useInterval` フック（Dan Abramov のパターン）を導入することで、タイマーロジックを宣言的に記述できるようになり、コンポーネントのクリーンアップ漏れや副作用の複雑さを大幅に削減できた。
- 秒数を「MM:SS」や「M分S秒」に変換するロジックが各 所に散在していた。これらを `ageUtils.ts` に `formatTimeMMSS` および `formatSecondsToJapanese` として集約することで、表示の一貫性を確保し、重複コードを排除した。

アクション:
- 今後、新たにタイマー機能や秒単位の表示が必要な場合は、自作せずにこれらの共通フックとユーティリティを使用することを徹底する。
- 1分以上の更新（睡眠タイマーなど）でも `useInterval` を活用することで、`useEffect` 内での複雑な分岐を避け、可読性を高めることができる。

2026-02-23 - [共通化] UIテーマカラー設定の定数化 (UI_COLORS)

学び:
-  や  などのUIコンポーネント内に、複雑なカラー設定 オブジェクト（, ）がハードコードされており、可読性を低下させていた。
-  に  としてテーマカラー定義を一元化することで、コ ンポーネントから設定ロジックを分離し、アプリケーション全体での色の一貫性を担保しやすくなった。
- Tailwind CSSの動的クラス生成を避けつつ、定数オブジェクトとして色を管理することで、パージの問題を回避し、型安全性も確保できる。

アクション:
- 他のコンポーネント（例えば  や  など）でも同様のハードコードされた色設定が見られるため、順次  を参照するようにリファクタリングを進める。

2026-02-23 - [共通化] UIテーマカラー設定の定数化 (UI_COLORS)

学び:
- `TipsCard` や `StatsBlock` などのUIコンポーネント 内に、複雑なカラー設定オブジェクト（`colorConfig`, `colorStyles`）がハードコードされており、可読性を低下させていた。
- `frontend/constants/ui-colors.ts` に `UI_COLORS`  としてテーマカラー定義を一元化することで、コンポーネントから設定ロジックを分離し、アプリケーション全体での色の一貫性を担保しやすくなった。
- Tailwind CSSの動的クラス生成を避けつつ、定数オブジェクトとして色を管理することで、パージの問題を回避し、型安全性も確保できる。

アクション:
- 他のコンポーネント（例えば `WidgetCard` や `FeedingForm` など）でも同様のハードコードされた色設定が見 られるため、順次 `UI_COLORS` を参照するようにリファ クタリングを進める。

2024-05-24 - [カスタムフックによる非同期アクションの共通化]
学び: ダッシュボードの複数のウィジェット(Feeding, Diaper, Sleep)で、API呼び出し時のローディング状態管理 、エラーハンドリング、トースト通知のロジックが重複していた。これらはUIの違いはあるものの、「非同期アクションを実行し、結果をユーザーに通知する」という振る舞いは同一だった。
アクション: `useAsyncAction` フックを作成し、これら の共通ロジックを抽出した。これにより、コンポーネント側は「何をするか（API呼び出し）」と「どう通知するか （メッセージ）」に集中でき、状態管理のボイラープレートを排除できた。今後も同様のパターン（設定変更画面など）でこのフックを再利用できる可能性がある。

2026-02-23 - [型定義] データフェッチ用フック warehouse の型安全化と共通化

学び:
- `useData.ts` 内でインラインの `as` キャストが多用 されており、型安全性が損なわれていた。特にAPIの変更 に対する耐性が低かった。
- OpenAPIから生成された型定義 (`FamilyResponse` 等) があるにもかかわらず、手動で定義された部分的なインターフェース (`Member` 等) がコンポーネント内に散在し ており、整合性が取れていなかった。
- `useBabyResource` のような共通ロジックを独立したフックとして切り出すことで、再利用性とテスト容易性が向上した。

アクション:
- 今後新しいデータフェッチフックを作成する際は、必ず `types/generated/api.d.ts` の型を使用し、インラインでの型定義を避ける。
- コンポーネント内でローカルに定義されているインターフェースを見つけたら、共通の型定義に置き換えるリファクタリングを継続する。

2026-02-23 - [共通化] Dashboard Widgetのデータ表示部分の共通化

学び:
- `FeedingWidget`, `DiaperWidget`, `SleepWidget` で 、経過時間やデータなし時の表示、今日の回数などのUI構造が完全に重複していた。
- `WidgetContent` コンポーネントを作成し、これらの表示ロジックを集約することで、各ウィジェットのコード量を削減し、一貫性を確保した。
- `SleepWidget` のような特殊な表示（「睡眠中」など）が必要な場合でも、`children` や `emptyContent` など のPropsを柔軟に設計することで、共通コンポーネントを 適用できる。

アクション:
- 他のウィジェット（例えば `GrowthWidget` や `NoteWidget`）でも同様のパターンが見られる場合、`WidgetContent` の適用を検討する。
- 共通コンポーネントのProps設計では、使用側の型定義 （今回は `BaseWidgetProps`）との整合性を意識し、必要に応じて `optional` にするなど柔軟性を持たせる。

2026-02-23 - [共通化] フォーム共通スタイル (UI_FORMS, UI_BUTTONS) の抽出と定数化

学び:
- 授乳、おむつ、睡眠、メモなどの各記録フォームにおいて、プライマリボタンのスタイルや選択肢ボタン（Active/Inactive）のTailwindクラスが重複してハードコードさ れていた。
- `UI_COLORS` を拡張し、機能横断的な共通パターンを `UI_BUTTONS`（プライマリボタン）や `UI_FORMS`（タイマーコンテナ、選択ボタン）として定数化することで、個別のコンポーネントからスタイリングの詳細を分離できた。
- ダークモード対応（例: `dark:bg-indigo-500`）を含むスタイルを一元管理することで、将来的なテーマ変更や一貫性の維持が劇的に容易になった。
- `cn` ユーティリティと定数を組み合わせることで、コ ンポーネント固有のスタイル（マージンやパディング）と共通のテーマカラーを安全に合成できるパターンを確立した。

アクション:
- 他の入力フォーム（プロフィール編集、設定画面など）でも、ハードコードされたボタンや入力フィールドのスタイルを順次これらの共通定数に置き換える。
- 新しい機能を追加する際は、`UI_COLORS` に定義された既存のパターンに適合するかまず確認し、必要に応じて共通定数を拡張する。

2026-02-23 - [共通化] 時間フォーマット関数の集約

学び:
- `ContractionHistory.tsx` 内でローカルに定義されていた時間フォーマット関数 (`formatTime`, `formatDuration`) が、`ageUtils.ts` の既存のユーティリティや汎用的なフォーマットと重複していた。
- `ageUtils.ts` に `formatTimeHHMM` を追加し、`ContractionHistory.tsx` から参照するようにしたことで、コードの重複を排除し、フォーマットの一貫性を向上させた。
- 秒数を「M分S秒」形式にするロジックも `formatSecondsToJapanese` として統一できた。

アクション:
- 他のコンポーネントでも時間フォーマットを独自に行っている場所があれば、`ageUtils.ts` の関数に置き換える。

2026-02-23 - [Refactor] SleepWidgetのフック統合

学び:
- `SleepWidget` が `usePermissions` と `useAsyncAction` を個別に呼び出しており、他のウィジェット（`FeedingWidget`, `DiaperWidget`）で使用されている `useQuickRecord` パターンと異なっていた。
- `useQuickRecord` を適用することで、権限チェック、ローディング状態管理、API呼び出しのボイラープレートを削減し、コードの一貫性を向上させた。
- `useQuickRecord` は `feedbackType` がなくても汎用的に使用できることが確認できた。

アクション:
- 他のウィジェットや機能でも、同様の「権限チェック + 非同期アクション」のパターンがあれば、`useQuickRecord` の適用を検討する。

2026-02-23 - [共通化] NoteWidgetとGrowthWidgetの標準化

学び:
-  と  は  を完全に適用するのは難しかったが、 と  を適用することで、他のウィジェットとの整合性を高めることができた。
-  を直接使用せず、 を使用することで、ローディングUIの実装を統一した。
- 「部分的な共通化」でも、型定義やメモ化ロジックの重複を排除できるため、十分に価値がある。

アクション:
- 他のダッシュボードコンポーネントでも、完全に共通化できない場合でも、Props定義やユーティリティ関数のレベルでの共通化を模索する。


2026-02-23 - [共通化] NoteWidgetとGrowthWidgetの標準化

学び:
- `NoteWidget` と `GrowthWidget` は `WidgetContent` を完全に適用するのは難しかったが、`BaseWidgetProps` と `createWidgetMemoComparison` を適用することで、他のウィジェットとの整合性を高めることができた。
- `BabyBottleLoading` を直接使用せず、`WidgetLoading` を使用することで、ローディングUIの実装を統一した。
- 「部分的な共通化」でも、型定義やメモ化ロジックの重複を排除できるため、十分に価値がある。

アクション:
- 他のダッシュボードコンポーネントでも、完全に共通化できない場合でも、Props定義やユーティリティ関数のレベルでの共通化を模索する。

2026-02-23 - [Dashboard] 不要な再レンダリングとデッドコードの排除
学び: DashboardPageでアロー関数を使ってコールバックを渡していたため、Memo化されたウィジェットコンポーネントが機能していなかった。また、ウィジェット内のボタンに不要なイベント伝播阻止コードが含まれていた。
アクション: Memo化されたコンポーネントにコールバックを渡す際はuseCallbackを使用し、参照等価性を保つ。不要なイベントハンドリングコードは定期的に見直して削除する。

2026-02-23 - [共通化] Dashboard Widgetと詳細ページの統計ロジック重複の排除

学び:
- ダッシュボードのウィジェット（`DiaperWidget`）と詳細ページの統計表示（`DiaperStats`）で、全く同じ統計計算ロジック（おしっこ・うんちの回数、経過時間）が重複して実装されていた。
- それぞれが異なるデータ型（`UnifiedRecord` vs `Diaper` Entity）を使用しているため、共通化が難しかったが、アダプター関数（`normalizeDiaperFromRecord`, `normalizeDiaperFromEntity`）を導入してデータを正規化することで、計算ロジックを `calculateDiaperStats` として一元化できた。
- 異なるコンテキスト（ダッシュボードの一括取得データと詳細ページの個別データ）でも、データの正規化層を挟むことでドメインロジックを再利用できるパターンを確立した。

アクション:
- 授乳（Feeding）や睡眠（Sleep）など、他の記録タイプでも同様の「ウィジェット vs 詳細ページ」の重複パターンがあるため、この正規化アダプターパターンを適用してリファクタリングを進める。

2026-02-24 - [共通化] 授乳統計ロジックの正規化と共通化

学び:
- `FeedingWidget` と `useFeeding` フックで、授乳データの統計計算ロジック（今日の回数、経過時間など）が重複しており、データの型も異なっていた（`BabyRecord` vs `Feeding`）。
- `diaperUtils` のパターンに倣い、`NormalizedFeeding` 型と正規化関数（`normalizeFeedingFromRecord`, `normalizeFeedingFromEntity`）を導入することで、異なる入力ソースから同一の統計ロジック（`calculateFeedingStats`）を利用できるようになった。
- `FeedingStats` コンポーネントのプロップスを `snake_case` から `camelCase` に変更することで、TypeScript/JavaScript の標準的な命名規則に準拠させ、可読性を向上させた。

アクション:
- まだ `Growth` や `Sleep` に同様の重複が残っている可能性があるため、順次この「正規化アダプターパターン」を適用していく。
2026-02-21 - [Fat Componentの分割] LandingContent.tsxのリファクタリング
学び: 巨大なランディングページのコンポーネントは、セクションごとに分割することで可読性が劇的に向上する。静的なコンテンツが多い場合、各セクションを独立したコンポーネントとして切り出すことで、親コンポーネントが単なるレイアウト構成要素となり、保守性が高まる。
アクション: ページのメインコンポーネントが500行を超えた場合、セクション単位での分割を積極的に検討する。
