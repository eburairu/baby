2023-10-25 - [React Hooks]
学び: setTimeoutをuseEffect内で使用する際、アンマウント時のクリーンアップ処理（clearTimeout）を忘れると、アンマウント済みのコンポーネントに対する状態更新（setState）が発生し、メモリリークの原因となる。
アクション: 今後、useEffect内で非同期処理（タイマーやイベントリスナー）を登録する場合は、必ずクリーンアップ関数を返す設計にする。

2023-10-25 - [React Hook Form]
学び: React Hook Formの `form.watch` をコンポーネントのトップレベルで直接呼び出すと、値の変更ごとにコンポーネント全体が再レンダリングされてしまう。また、React Compiler（React19以降の実験的機能など）において「incompatible library」の警告を引き起こす。
アクション: 特定のフィールド値のみを監視したい場合は、常に `useWatch({ control, name })` を使用し、不要な再レンダリングを防ぎ、コンパイラの警告を解消する。

2023-10-25 - [TypeScript]
学び: zodResolverを使用する際に型エラーを回避する目的で `as any` を安易に使用すると、TypeScriptの厳密な型チェックが機能しなくなる。
アクション: どうしても型の不一致を解消する必要がある場合は、`as unknown as Resolver<FormValuesType>` のように一旦 `unknown` を経由して目的の型にキャストすることで、anyの利用を避け、型安全性を確保する。
### アクション:
- 今後新しいフォームを作成する際は、選択肢定義を `constants` ディレクトリに配置することを推奨する。
- `useInterval` などの副作用を伴うロジックは、積極的にカスタムフックに隠蔽する。

## 2026-02-23 - [静的レイアウトと共通フックの整理]
### 学び: ページ共通レイアウトとフック再利用の重要性
- **コード構造**: 静的ページ（利用規約、プライバシーポリシー等）は一貫したレイアウトを持つべきだが、コピー＆ペーストで実装されていたため、デザイン変更時に修正漏れが発生するリスクがあった。`StaticPageLayout` として共通化することで保守性が向上した。
- **フック再利用**: `ContractionHistory` 内で削除・コメントロジックが独自実装されていたが、`useRecordDelete` や `useRecordComments` といった既存の共通フックに置き換えることで、コード量を削減し、一貫したUX（削除確認ダイアログなど）を提供できた。

### アクション:
- 新しいページを作成する際は、既存のレイアウトコンポーネントを積極的に利用する。
- リソース操作（CRUD）を行うコンポーネントを見つけたら、既存のカスタムフック（`useRecordDelete` 等）が適用できないか検討する。

## 2026-02-24 - [Fat Componentの分割とディレクトリ構造の明確化]
### 学び: コンポーネントの責務分離とユーティリティの一元化
- **Fat Componentの分割**: `ContractionHistory.tsx` にリストレンダリング、編集ダイアログ、削除ロジックが混在していた。`ContractionHistoryItem`（表示）と `ContractionEditDialog`（編集）に分離することで、親コンポーネントの見通しが良くなり、各コンポーネントの責務が明確になった。
- **ディレクトリ構造の明確化**: `frontend/utils` と `frontend/lib` が混在し、開発者がどちらを使うべきか迷う状態だった。`utils` の内容を `lib` に統合することで、ファイルの配置基準が統一され、迷いがなくなった。

### アクション:
- 長大なリストレンダリングや複雑なダイアログを持つコンポーネントは、積極的にサブコンポーネントに切り出す。
- 似た役割を持つディレクトリ（例: utils/lib, helpers/services）が乱立していないか定期的に見直し、統合する。

## 2026-02-26 - [履歴リストのメタデータ共通化と日付フォーマット集約]
### 学び: 微細なUI重複の排除とフォーマットの一元管理
- **UI重複**: `FeedingHistory`, `SleepHistory`, `DiaperHistory`, `NoteHistory` の各リストアイテム内で、記録者名とコメントボタンの表示ロジックが完全に重複していた。これらは `RecordMetaItems` として切り出すことで、各コンポーネントの記述量を減らし、将来的なデザイン変更（アイコン変更など）を容易にした。
- **フォーマット集約**: `date-fns` の `format` 関数とロケール指定が各コンポーネントに散らばっていた。`dateUtils` を導入しラップすることで、日付フォーマットの仕様変更に強くし、インポート文を簡素化した。
- **部分的な共通化**: アイコン表示ロジック (`RecordIcon`) も共通化を試みたが、`FeedingHistory` のようにレコードタイプ内でさらに条件分岐（母乳/ミルク）がある場合、無理に共通コンポーネントを使うと柔軟性が失われる可能性がある。共通化できる部分（メタデータ）と、コンポーネント固有のロジックを残すべき部分を見極めることが重要。

### アクション:
- リスト表示など、繰り返し現れるUIパターンは即座に共通コンポーネント化を検討する。
- 日付や通貨などのフォーマット処理は、直接ライブラリを呼ばず、プロジェクト固有のユーティリティを通すようにする。
2024-05-22 - [Hooks & Components De-cluttering] 学び: Fat Hook（useData.ts）やFat Component（FeedingForm.tsx）は、責務の分離原則に基づいて分割することで、見通しが劇的に良くなる。特にReact Hook FormとSWRの組み合わせでは、API呼び出しと状態管理が密結合になりがちだが、UIコンポーネント（Selectionなど）を切り出すだけでも可読性は向上する。 アクション: 次回はAPI呼び出しロジック自体もカスタムフック（useFeedingFormLogicなど）に完全に分離し、ViewとLogicを完全に分けたい。

## 2026-02-28 - [不要になった再エクスポートファイルの削除と直接インポートへの切り替え]
### 学び: 再エクスポートファイルへの依存と間接参照の排除
- **コード構造**: 以前のFat Hook（`useData.ts`）を個別のフックに分割した際、後方互換性のために `useData.ts` が再エクスポート用のファイルとして残されていた。多くのコンポーネントがこの再エクスポートファイルに依存し続けていたため、不要な間接参照が発生していた。
- **リファクタリング**: `useData.ts` に依存している全てのファイル（ページ、コンポーネント、他のフック）のインポートパスを、個別のフック（`useBabies`, `useSleep`, `useDiaper` など）に直接向くよう修正し、`useData.ts` 自体を削除した。これにより、依存関係がより明確になり、不要なファイルが1つ減った。

### アクション:
- モジュールの分割やリファクタリングを行う際、一時的に再エクスポートを利用して後方互換性を保つのは有効な手段だが、長期間残すべきではない。
- チーム内の他の開発者が新しいフックの位置に慣れる期間を置いた後、適切に一斉置換を行い、再エクスポートファイルを削除してクリーンアップするフェーズを計画的に組み込む必要がある。

## 2025-02-28 - [React Hook Formのwatch最適化]
### 学び: useWatchを使った再レンダリングの抑制
- **コード構造**: `DiaperForm`, `MilestoneForm`, `VaccinationForm` など、React Hook Formを使用しているコンポーネントで `form.watch` が直接使用されていた。これにより、フォームの値が変更されるたびにコンポーネント全体が再レンダリングされる可能性があり、React Compiler から警告（`react-hooks/incompatible-library`）が出ていた。
- **リファクタリング**: `form.watch("field_name")` を `useWatch({ control: form.control, name: "field_name" })` に置き換えることで、特定のフィールドの変更のみを監視し、不要な再レンダリングを抑制できるようになった。これにより、警告も解消され、パフォーマンスとコンパイラの互換性が向上した。

### アクション:
- 今後React Hook Formで特定のフィールドの値を監視する場合は、`form.watch` ではなく `useWatch` フックを積極的に使用し、コンポーネントの再レンダリングを最適化する。

2024-06-25 - [Tidy Refactor: UI共通化とビジネスロジックの分離]
学び:
1. DiaperFormにおいて、同一のUIを持つボタンのマークアップとTailwindのクラス定義（特にcvaを使わず動的クラス生成している箇所）が重複して記述されていた。同じような「選択肢から選ぶ」フォームUIは他のトラッカー（例えば睡眠、授乳）にも存在するため、共通の「選択肢ボタン」サブコンポーネント化（`DiaperTypeButton`等）は可読性向上に直結する。
2. フォームデータの処理（特に`onSubmit`内での`vals.notes`の文字列結合のような複雑なデータ生成）は、`submitRecord`のペイロードジェネレーターに直接記述すると関数の肥大化を招く。これらを純粋関数（`generateDiaperNotes`）として外部に抽出することで、テスト容易性と可読性が向上する。
アクション: 今回は`DiaperForm.tsx`内部でのサブコンポーネント化と関数抽出に留めたが、`DiaperTypeButton`のようなRadio Button風のカスタムUIは、今後`frontend/components/ui/`配下の汎用コンポーネント（例: `SelectionButton`）として再設計し、他ドメインのフォーム（授乳、睡眠など）でも再利用可能か検討する。また、ペイロード構築ロジックは、今後ドメイン固有のutilsファイルへの移動を検討する。
2025-02-28 - [Tidy: 設定画面のコンポーネント抽出とダミータイマーの教訓]
学び:
1. 複数の類似したUI要素（アイコン付きのリスト項目）がページコンポーネント内にインラインで記述されているアンチパターンを発見。これを `SettingItem` コンポーネントとして抽出することで、コード行数を大幅に削減し、DRY原則を適用できた。
2. ダッシュボードのウィジェット（`FeedingWidget`、`DiaperWidget`）にある一見不要に見える `tick` 状態（未使用のタイマー変数）は、単なるアンチパターンではなく、「現在時刻からの経過時間」などをコンポーネントに定期的に再計算・再レンダリングさせるための必須ハックである。これをクリーンアップと勘違いして削除すると、UIが更新されなくなるバグ（機能後退）を引き起こす。
アクション:
- 今後UIコンポーネントのリファクタリングを行う際は、明示的に消費されていない状態（state）であっても、再レンダリングのトリガーとしての意図的な役割（ダミータイマーなど）を持っていないか慎重に分析する。
- ページコンポーネントの行数が200行を超えるような場合は、積極的にプレゼンテーション要素（UI）を小さなコンポーネントに切り出す方針を継続する。

## 2025-03-01 - [フォーム再レンダリング最適化と履歴リストUIの共通化]
### 学び: useWatch の活用と共通フック・コンポーネントへの集約
- **パフォーマンス最適化**: `BottleFeedingFields.tsx` にて、`form.watch("bottle_content_type")` を使用していた箇所を `useWatch` に置き換えた。`form.watch` はフォーム全体または親コンポーネントの再レンダリングを引き起こす可能性があるが、`useWatch` を使うことで監視対象のフィールド変更時のみの再レンダリングに抑えることができ、パフォーマンスが向上する。
- **UIとロジックの共通化**: `NoteHistory.tsx` にて、以前の `DiaperHistory` や `SleepHistory` で行われたリファクタリングが適用されておらず、削除確認ダイアログ・コメントダイアログ・リストアイテムのUIが独自実装されていた。これを `useRecordDelete`, `useRecordComments`, `HistoryCard`, `RecordListItem`, `RecordActionButtons` 等の共通モジュールに置き換えることで、コード行数を大幅に削減（約270行→220行）し、アプリ全体でのUI/UXの一貫性を確保できた。

### アクション:
- 今後も React Hook Form を使用する際、特定のフィールド値のみを監視してUIを切り替える場合は `useWatch` を標準として利用する。
- 新しいトラッカーや履歴画面を追加する際は、必ず `components/records/` 配下の共通コンポーネントと、`hooks/useRecordDelete`, `useRecordComments` などの共通フックを使用する。

2024-03-01 - [ダッシュボードウィジェットの時間更新ロジックの共通化と画像最適化]
学び: 複数のダッシュボードウィジェット（FeedingWidget, DiaperWidget, SleepWidget）で、時間経過をリアルタイムにUIへ反映させるためだけに `useState` と `setInterval` + `useEffect` のセットが各ファイルで重複して実装されていました。これはコードの冗長性を生み、保守性を下げるアンチパターンです。また、LandingHeroコンポーネントで標準の `<img>` タグが使用されており、Next.jsのESLint警告（no-img-element）が出現していました。
アクション: 時間更新のためのロジックをラップした `useTick` フックを新規作成し、各ウィジェットでこのフックを呼び出す形にリファクタリングしました。これにより、不要なライフサイクル管理コードを排除し、DRY原則を適用できました。今後は、同様の「単なるポーリングや時間更新」目的のState+Effectの組み合わせを見つけたら、`useTick` や `useInterval` のような共通フックへの移行を検討します。また、`<img>` タグを `next/image` に置き換えました。

2026-03-01 - [EditDialogBaseへの集約とFat Componentの抽出]
学び: 複数のドメイン（陣痛、成長、メモ等）で編集ダイアログや新規作成ダイアログが `Dialog` コンポーネントを直接使用しており、`EditDialogBase` のような共通ラッパーが活用されていなかった。また、`NoteHistory.tsx` 内に直接フォームとダイアログのロジックが記述されており、履歴リストの表示とデータ編集の責務が混在していた。これらを分離することで可読性が向上した。さらに、`GrowthHistoryList` がテーブル表示のみを行っていたため、他の履歴コンポーネントで使われている `HistoryCard` によるラップを追加し、ダッシュボードや履歴ページ全体でのカードUIの一貫性を確保した。
アクション: 新しいダイアログを作成する際は、生コンポーネント（`Dialog`など）を直接使うのではなく、アプリケーションで定義された共通ベースコンポーネント（`EditDialogBase`など）を使用することを標準とする。また、履歴やリストを表示するコンポーネント内に、データ編集用のフォームロジックがインラインで記述されている場合は、独立したコンポーネント（`*EditDialog` 等）として切り出す。

## 2026-03-01 - [日付フォーマット処理のdateUtils集約]
### 学び: date-fns の分散とユーティリティの拡充
- **コード構造**: `date-fns` の `format` 関数と `ja` ロケール、および `yyyy-MM-dd'T'HH:mm` などのマジックストリングが複数のコンポーネント（フォームの初期値設定や表示）に散在していた。これにより、フォーマット変更時の修正漏れリスクとコードの冗長性が高まっていた。
- **リファクタリング**: `frontend/lib/dateUtils.ts` に `formatDateTimeLocal`, `formatDateLocal`, `formatJapaneseDate` を追加し、`NoteForm`, `RecordDetailDialog`, `AppInfoDialog` の `date-fns` 直接インポートを置き換えた。これにより、日付操作のロジックが1箇所に集約され、コンポーネントの可読性が向上した。

### アクション:
- 今後新しいコンポーネントで日付フォーマットが必要になった際は、`date-fns` を直接使うのではなく、必ず `dateUtils.ts` のユーティリティ関数を使用するか、必要に応じてユーティリティ側に新しいフォーマット関数を追加する。

## 2026-03-01 - [日付フォーマット処理のdateUtils集約の徹底とマジックストリングの排除]
### 学び: date-fns の分散とユーティリティの拡充
- **コード構造**: 以前のリファクタリングで `dateUtils.ts` に日付フォーマット処理を集約したが、依然として複数のフォーム（`DiaperForm`, `FeedingForm`, `SleepForm`, `GrowthRecordForm`, `MilestoneForm`, `VaccinationForm`）において `date-fns` を直接インポートし、`yyyy-MM-dd'T'HH:mm` や `yyyy-MM-dd` などのマジックストリングを使用して日付をフォーマットしている箇所が多数残っていた。
- **リファクタリング**: `dateUtils.ts` の `formatDateTimeLocal` と `formatDateLocal` を使用するように各フォームを修正し、`date-fns` の直接インポートとマジックストリングを排除した。これにより、アプリ全体での日付フォーマットの一貫性がさらに強化され、将来的なフォーマット変更に対する耐性が高まった。

### アクション:
- 新規フォーム作成時や既存コードの改修時に `date-fns` を直接インポートしている箇所を見つけた場合は、必ず `dateUtils.ts` を経由するようにリファクタリングを継続する。

2024-03-02 - [クリップボードコピーツールの共通化 (`useClipboard` の作成)]
学び: クリップボードへのテキストコピーと、コピー完了メッセージの一時表示 (setTimeout) という定型的なUI処理が、複数のコンポーネントで個別に実装されていた。これは重複コードであり、将来的な仕様変更（例えばコピーのタイムアウト時間を変更するなど）の際に修正漏れが発生する原因となる。
アクション: `useClipboard` カスタムフックを作成し、状態管理とクリップボード操作を隠蔽した。今後、同様の「テキストをコピーして一時的にメッセージを表示する」機能が必要になった場合は、この共通フックを利用することで実装の重複を防ぎ、一貫性を保つ。
## 2026-03-02 - [Tidy: リファクタリング - 日付フォーマットの共通化]
**学び:** コードベース全体で `format(new Date(), ...)` と date-fns の `ja` ロケールが各コンポーネントに直接インポートされ、散在しているパターン（アンチパターン）を発見しました。これにより、同じフォーマット文字列（例: `"MM/dd"`, `"PPP"`）が複数箇所にハードコードされ、保守性と可読性が低下していました。
**アクション:** `frontend/lib/dateUtils.ts` に `formatMonthDay`, `formatJapaneseDatePPP`, `formatDateWithWeekday` などの意味のある名前を持つラッパー関数を追加し、これらを利用するように各コンポーネントをリファクタリングしました。今後も日付フォーマットは `dateUtils.ts` に集約し、コンポーネント側からは直接 date-fns を呼び出さないよう徹底します。

## 2026-03-02 - [Tidy: リファクタリング - date-fns の依存排除と useEffect + setInterval の共通化]
**学び:** `GrowthChart` や `feeding-stats` において、`date-fns` の `subMonths`, `subDays`, `formatDistanceToNow` といった関数が直接インポートされていました。これによりコード内でライブラリへの依存が分散し、将来的なバージョンアップや他のライブラリへの乗り換えが困難になります。また `useFeedingTimer` において、`useEffect` 内部で `setInterval` を利用し手動でクリーンアップを行っていましたが、プロジェクト内にはすでに `useInterval` フックが存在しており、これを活用していない冗長な記述でした。
**アクション:** `dateUtils.ts` に `subtractMonths`, `subtractDays`, `formatTimeAgo` のラッパーを追加し、これらを経由して `date-fns` を利用するように修正しました。また、インターバル処理は必ず `useInterval` などの専用カスタムフックを使用し、ライフサイクル管理を宣言的かつ安全に行うように統一します。

## 2026-03-02 - [Tidy: リファクタリング - useWatchの正しい適用とFat Componentの分割]
**学び:** `form.watch` を `useWatch` に置き換える最適化は、フォームコンポーネントのトップレベルで呼び出してしまうと、結果的に親コンポーネント全体が再レンダリングされるため、パフォーマンス改善の恩恵を十分に受けられない（アンチパターン）ことが分かりました。これを防ぐには、監視対象のフィールドに依存する部分だけを別コンポーネントに切り出し、その内部で `useWatch` を呼び出す必要があります。
**アクション:** `DiaperForm`, `MilestoneForm`, `VaccinationForm` において、フィールドの表示/非表示や活性状態が他フィールドに依存している箇所を、それぞれ `PoopDetailsFields`, `MilestoneTitleField`, `VaccinationCompletedFields` として切り出し、フォーム全体の無駄な再レンダリングを防止しました。今後も `useWatch` を導入する際は、適用範囲を限定するためのサブコンポーネント化をセットで行うことを標準とします。
## 2026-03-02 - [Tidy: リファクタリング - ダイアログの共通化と定数の集約]
**学び:** ダッシュボードの `RecordDetailDialog` において、共通の `EditDialogBase` が使用されておらず、またネイティブの `confirm()` が使用されていました。さらに、`ActivityItem` 内で背景色の定数 (`RECORD_TYPE_BG_COLORS`) がインラインで定義されていました。これらはDRY原則とUIの一貫性を損なうアンチパターンです。
**アクション:** `RecordDetailDialog` を `EditDialogBase` に置き換え、`AlertDialog` による削除確認を導入しました。また `RECORD_TYPE_BG_COLORS` を `frontend/constants/ui.ts` に集約し、コンポーネント間で再利用可能にしました。今後も共通ベースコンポーネントの使用と定数の外部化を徹底します。
