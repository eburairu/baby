2026-02-23 - [抽出] 授乳タイマーロジックのカスタムフック化

学び:
- `FeedingForm` コンポーネント内に複雑なタイマーロジック（`setInterval`, `useRef`, 状態管理）が混在しており、可読性が低下していた。
- `useFeedingTimer` フックにロジックを抽出することで、コンポーネントはUIとフォームの状態管理に集中できるようになった。
- フックが純粋な状態と操作を返し、フォームとの同期はコンポーネント側の `useEffect` で行うことで、フックの再利用性を高め、依存関係を疎に保つことができた。

アクション:
- 他のタイマー機能や複雑な状態管理を持つコンポーネント（例: 陣痛タイマーなど）でも同様のパターンを適用できるか検討する。
- フォームの値と外部の状態（タイマーなど）を同期させる際は、方向（State -> Form か Form -> State か、あるいは双方向か）を意識し、ループが発生しないように注意する。

2026-02-23 - [共通化] Dashboard Widgetのメモ化と比較ロジックの共通化

学び:
- 複数のダッシュボードウィジェットで `React.memo` を使用した再レンダリング制御ロジックが重複していた。
- 共通のProps型 (`BaseWidgetProps`)、メモ化ロジックファクトリ (`createWidgetMemoComparison`)、およびローディングUI (`WidgetLoading`) を導入することで、各ウィジェットの実装を大幅に簡素化した。
- 高階コンポーネント (HOC) を強制的に適用するよりも、ヘルパー関数として比較ロジックを提供する方が、コンポーネントの柔軟性を保ちつつ重複を排除できる。

アクション:
- 他の類似構造を持つUIコンポーネント（例えば設定画面の各セクションなど）でも、Propsの共通化とロジックの抽出を検討する。

2026-02-23 - [共通化] APIクライアントの重複コードのリファクタリング

学び:
- `frontend/lib/api.ts` の各HTTPメソッド（`api.post`, `api.put` など）で `fetch` 呼び出しやエラーハンドリングのコードが重複しており、冗長だった。
- 共通の `request` ヘルパー関数にロジックを集約することで、コードベースを削減し、メンテナンス性を向上させた。
- Node.js 環境（テスト環境）での `Headers` オブジェクトの挙動（キーが小文字に正規化される）により、テストの期待値が失敗した。実装を無理に合わせるのではなく、テストコードを正しい挙動に合わせて修正することが重要だった。

アクション:
- 他のユーティリティ関数（例えばデータ変換ロジックなど）でも、同様の重複パターンがないか確認し、共通関数への抽出を進める。
2025-05-24 - [Refactor] GrowthRecordFormの標準化
学び: GrowthRecordForm.tsx は shadcn/ui の Form コンポーネントを使用せず、独自の実装を行っていたため、コードベース全体の一貫性が損なわれていた。
また、useEffect による手動のリセットロジックは React Hook Form の values オプションを使用することで簡素化できる。
アクション: GrowthRecordForm.tsx を shadcn/ui の Form コンポーネントを使用するようにリファクタリングし、React Hook Form のベストプラクティスに従う。これにより、コードの可読性が向上し、ボイラープレートコードが削減される。

2026-02-23 - [Refactor] BabyFormのshadcn/ui化と非推奨パターンの排除

学び:
- `BabyForm` コンポーネントが標準的な `shadcn/ui` の `Form` コンポーネントを使用しておらず、一貫性が欠けていた。
- `useEffect` を使用したフォーム値のリセットは、`react-hook-form` の `values` オプションを使用することで宣言的に記述でき、複雑さを排除できる。
- `Controller` パターン (`FormField`) を使用することで、`RadioGroup` などの制御コンポーネントも統一的に扱える。

アクション:
- 他のフォームコンポーネントでも同様の `useEffect` によるリセットパターンがないか確認し、あれば `values` オプションへの置き換えを推奨する。

2026-02-23 - [共通化] 統計情報の表示ブロック (StatsBlock) の共通化

学び:
- 授乳、睡眠、排泄など、複数の機能で「アイコン付きの統計カード」というUIパターンが繰り返されていた。
- `StatsBlock` コンポーネントを作成し、カラーバリエーションを持たせることで、各機能のテーマカラーを維持しつつ実装を共通化できた。
- 完全に同一の構造ではなくても、`children` を活用して可変部分（サブテキストや追加情報）を受け入れることで、柔軟な共通コンポーネントを作成できる。

アクション:
- Dashboard Widgetの内部構造にも似たパターンがある可能性があるため、将来的に `StatsBlock` または類似のコンポーネントを適用できるか検討する。

2026-02-23 - [共通化] UIテーマカラー設定の定数化 (UI_COLORS)

学び:
-  や  などのUIコンポーネント内に、複雑なカラー設定オブジェクト（, ）がハードコードされており、可読性を低下させていた。
-  に  としてテーマカラー定義を一元化することで、コンポーネントから設定ロジックを分離し、アプリケーション全体での色の一貫性を担保しやすくなった。
- Tailwind CSSの動的クラス生成を避けつつ、定数オブジェクトとして色を管理することで、パージの問題を回避し、型安全性も確保できる。

アクション:
- 他のコンポーネント（例えば  や  など）でも同様のハードコードされた色設定が見られるため、順次  を参照するようにリファクタリングを進める。

2026-02-23 - [共通化] UIテーマカラー設定の定数化 (UI_COLORS)

学び:
- `TipsCard` や `StatsBlock` などのUIコンポーネント内に、複雑なカラー設定オブジェクト（`colorConfig`, `colorStyles`）がハードコードされており、可読性を低下させていた。
- `frontend/constants/ui-colors.ts` に `UI_COLORS` としてテーマカラー定義を一元化することで、コンポーネントから設定ロジックを分離し、アプリケーション全体での色の一貫性を担保しやすくなった。
- Tailwind CSSの動的クラス生成を避けつつ、定数オブジェクトとして色を管理することで、パージの問題を回避し、型安全性も確保できる。

アクション:
- 他のコンポーネント（例えば `WidgetCard` や `FeedingForm` など）でも同様のハードコードされた色設定が見られるため、順次 `UI_COLORS` を参照するようにリファクタリングを進める。

2024-05-24 - [カスタムフックによる非同期アクションの共通化]
学び: ダッシュボードの複数のウィジェット(Feeding, Diaper, Sleep)で、API呼び出し時のローディング状態管理、エラーハンドリング、トースト通知のロジックが重複していた。これらはUIの違いはあるものの、「非同期アクションを実行し、結果をユーザーに通知する」という振る舞いは同一だった。
アクション: `useAsyncAction` フックを作成し、これらの共通ロジックを抽出した。これにより、コンポーネント側は「何をするか（API呼び出し）」と「どう通知するか（メッセージ）」に集中でき、状態管理のボイラープレートを排除できた。今後も同様のパターン（設定変更画面など）でこのフックを再利用できる可能性がある。
