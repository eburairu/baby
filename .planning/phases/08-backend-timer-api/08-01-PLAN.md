---
phase: 08-backend-timer-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_timer_contraction.py
  - tests/test_timer_feeding.py
autonomous: true
requirements:
  - TIMER-BE-01
  - TIMER-BE-02
  - TIMER-BE-03
  - TIMER-BE-04
  - TIMER-BE-05

must_haves:
  truths:
    - "GET /api/babies/{baby_id}/timer/contraction は status と start_time を返す"
    - "PUT /api/babies/{baby_id}/timer/contraction で status を idle/timing に更新できる"
    - "GET /api/babies/{baby_id}/timer/feeding は active_side・左右累積秒数・アクティブ区間開始時刻を返す"
    - "PUT /api/babies/{baby_id}/timer/feeding で授乳タイマー状態を更新できる"
    - "別家族のユーザーによるアクセスは 403 を返す"
  artifacts:
    - path: "tests/test_timer_contraction.py"
      provides: "陣痛タイマー API の TDD RED テスト群"
    - path: "tests/test_timer_feeding.py"
      provides: "授乳タイマー API の TDD RED テスト群"
  key_links:
    - from: "tests/test_timer_contraction.py"
      to: "GET/PUT /api/babies/{baby_id}/timer/contraction"
      via: "TestClient"
      pattern: "client\\.get.*timer/contraction"
    - from: "tests/test_timer_feeding.py"
      to: "GET/PUT /api/babies/{baby_id}/timer/feeding"
      via: "TestClient"
      pattern: "client\\.get.*timer/feeding"
---

<objective>
TDD RED フェーズ: 陣痛タイマーと授乳タイマー API の受け入れテストを先に記述する。

Purpose: テストを先に書くことで API 契約を仕様として固め、実装フェーズ（Plan 03）でテストを GREEN にする明確なゴールを設定する。
Output: 2 つのテストファイル（実行すると FAILED になることを確認済み）
</objective>

<execution_context>
@/Users/ry1e/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ry1e/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/conftest.py
@app/dependencies.py
</context>

<feature>
  <name>陣痛タイマー API テスト (test_timer_contraction.py)</name>
  <files>tests/test_timer_contraction.py</files>
  <behavior>
    - GET /api/babies/{baby_id}/timer/contraction — 初期状態: {"status": "idle", "start_time": null}
    - PUT /api/babies/{baby_id}/timer/contraction — body: {"status": "timing", "start_time": "2026-01-01T10:00:00Z"} → 200 で更新後の値を返す
    - PUT 後に GET すると更新された status と start_time が返る（同一ユーザー）
    - PUT で status: "idle"、start_time: null にリセットできる
    - 別家族ユーザーが GET すると 403
    - 未認証で GET すると 401
  </behavior>
  <implementation>
    conftest.py の auth_client フィクスチャを参考にテストユーザーと baby を作成する。
    別家族テストは family_name を変えた 2 つの auth_client で検証する。
    テスト実行後は FAILED になることを確認してコミット:
    `test(08-01): 陣痛タイマーAPI TDD RED テスト作成`
  </implementation>
</feature>

<feature>
  <name>授乳タイマー API テスト (test_timer_feeding.py)</name>
  <files>tests/test_timer_feeding.py</files>
  <behavior>
    - GET /api/babies/{baby_id}/timer/feeding — 初期状態: {"active_side": null, "left_elapsed_seconds": 0, "right_elapsed_seconds": 0, "segment_start_time": null}
    - PUT /api/babies/{baby_id}/timer/feeding — body: {"active_side": "LEFT", "left_elapsed_seconds": 30, "right_elapsed_seconds": 0, "segment_start_time": "2026-01-01T10:00:00Z"} → 200
    - PUT 後に GET すると全フィールドが更新されている
    - PUT で active_side: null, segment_start_time: null にリセットできる（left/right elapsed はゼロに戻せる）
    - 別家族ユーザーが GET すると 403
    - 未認証で GET すると 401
  </behavior>
  <implementation>
    test_timer_contraction.py と同じ conftest パターンを使用する。
    active_side は "LEFT" / "RIGHT" / null の 3 状態をカバーする。
    テスト実行後は FAILED になることを確認してコミット:
    `test(08-01): 授乳タイマーAPI TDD RED テスト作成`
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 陣痛タイマー API の RED テストを作成する</name>
  <files>tests/test_timer_contraction.py</files>
  <behavior>
    - test_get_contraction_timer_initial_state: GET で status=idle, start_time=null が返る
    - test_put_contraction_timer_start: PUT {status: timing, start_time: ...} で 200
    - test_put_then_get_contraction_timer: PUT 後の GET で値が反映されている
    - test_put_contraction_timer_reset: PUT {status: idle, start_time: null} でリセット
    - test_contraction_timer_cross_family_403: 別家族ユーザーが GET すると 403
    - test_contraction_timer_unauthenticated_401: 未認証で GET すると 401
  </behavior>
  <action>
    tests/test_timer_contraction.py を新規作成する。

    conftest.py の auth_client フィクスチャを使う。baby 作成は POST /api/babies/ を使う。
    別家族テストは auth_client(username="other_user", family_name="Other Family") で別クライアントを作成し、
    そのクライアントで baby_id にアクセスして 403 を確認する。

    エンドポイントは `/api/babies/{baby_id}/timer/contraction` (GET/PUT)。
    PUT のリクエストボディ: {"status": "timing", "start_time": "ISO8601文字列 or null"}
    GET のレスポンス: {"status": "idle"|"timing", "start_time": "ISO8601文字列 or null"}

    実装前なので全テストが FAILED になることを pytest で確認すること。
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby && python -m pytest tests/test_timer_contraction.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>全テストが FAILED/ERROR（404 または NotImplementedError）で終わり、テストロジック自体は正しい</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 授乳タイマー API の RED テストを作成する</name>
  <files>tests/test_timer_feeding.py</files>
  <behavior>
    - test_get_feeding_timer_initial_state: GET で active_side=null, left/right=0, segment_start_time=null が返る
    - test_put_feeding_timer_start_left: PUT {active_side: LEFT, ...} で 200
    - test_put_then_get_feeding_timer: PUT 後の GET で全フィールドが反映されている
    - test_put_feeding_timer_reset: PUT でリセット（active_side: null, elapsed: 0）
    - test_feeding_timer_cross_family_403: 別家族ユーザーが GET すると 403
    - test_feeding_timer_unauthenticated_401: 未認証で GET すると 401
  </behavior>
  <action>
    tests/test_timer_feeding.py を新規作成する。

    エンドポイントは `/api/babies/{baby_id}/timer/feeding` (GET/PUT)。
    PUT のリクエストボディ:
    {
      "active_side": "LEFT"|"RIGHT"|null,
      "left_elapsed_seconds": int,
      "right_elapsed_seconds": int,
      "segment_start_time": "ISO8601文字列 or null"
    }
    GET のレスポンス: 同じフィールド構造。

    実装前なので全テストが FAILED になることを pytest で確認すること。
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby && python -m pytest tests/test_timer_feeding.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>全テストが FAILED/ERROR で終わり、テストロジック自体は正しい</done>
</task>

</tasks>

<verification>
両テストファイルが存在し、pytest 実行で全テストが FAILED/ERROR になること（実装がないため）。
テストのロジック自体に構文エラーがないこと（import エラーは許容、404 は想定内）。
</verification>

<success_criteria>
- tests/test_timer_contraction.py が 6 テストを持ち、全 FAILED
- tests/test_timer_feeding.py が 6 テストを持ち、全 FAILED
- Plan 03 実装後に全テストが GREEN になれる仕様になっている
</success_criteria>

<output>
After completion, create `.planning/phases/08-backend-timer-api/08-01-SUMMARY.md`
</output>
