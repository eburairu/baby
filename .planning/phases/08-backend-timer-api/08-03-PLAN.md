---
phase: 08-backend-timer-api
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - app/routers/timer.py
  - app/main.py
  - frontend/openapi.json
autonomous: true
requirements:
  - TIMER-BE-01
  - TIMER-BE-02
  - TIMER-BE-03
  - TIMER-BE-04
  - TIMER-BE-05

must_haves:
  truths:
    - "GET /api/babies/{baby_id}/timer/contraction が status と start_time を返す"
    - "PUT /api/babies/{baby_id}/timer/contraction で status を idle/timing に更新でき、別ユーザーが GET すると更新後の値が返る"
    - "GET /api/babies/{baby_id}/timer/feeding が active_side・左右累積秒数・アクティブ区間開始時刻を返す"
    - "PUT /api/babies/{baby_id}/timer/feeding で授乳タイマー状態を更新でき、別ユーザーが GET すると更新後の値が返る"
    - "別家族のユーザーが baby_id にアクセスすると 403 が返る"
  artifacts:
    - path: "app/routers/timer.py"
      provides: "GET/PUT /api/babies/{baby_id}/timer/contraction と feeding のルーター"
      exports: ["router"]
    - path: "app/main.py"
      provides: "timer ルーターの登録"
      contains: "timer.router"
    - path: "frontend/openapi.json"
      provides: "最新の OpenAPI スキーマ（timer エンドポイント含む）"
  key_links:
    - from: "app/routers/timer.py"
      to: "app/models/timer.py"
      via: "ContractionTimerState, FeedingTimerState のインポートと upsert"
      pattern: "ContractionTimerState|FeedingTimerState"
    - from: "app/routers/timer.py"
      to: "app/dependencies.py"
      via: "verify_baby_access で family 境界を強制"
      pattern: "verify_baby_access"
    - from: "app/main.py"
      to: "app/routers/timer.py"
      via: "app.include_router(timer.router)"
      pattern: "include_router.*timer"
---

<objective>
タイマーステート API ルーターを実装し、main.py に登録して、テストを GREEN にする。

Purpose: Plan 01 の RED テストを GREEN にすることで、Phase 8 の全要件（TIMER-BE-01〜05）を満たす。
Output: app/routers/timer.py（新規）、app/main.py（更新）、frontend/openapi.json（更新）
</objective>

<execution_context>
@/Users/ry1e/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ry1e/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/dependencies.py
@app/models/timer.py
@app/schemas/timer.py
@app/routers/contraction.py
@app/main.py
@tests/test_timer_contraction.py
@tests/test_timer_feeding.py

<interfaces>
<!-- Plan 02 で作成したモデルとスキーマの契約 -->

From app/models/timer.py (Plan 02 で作成):
```python
class ContractionTimerState(Base):
    __tablename__ = "contraction_timer_states"
    id: int (PK)
    baby_id: int (FK babies.id, UNIQUE)
    status: str  # "idle" | "timing"
    start_time: datetime | None

class FeedingTimerState(Base):
    __tablename__ = "feeding_timer_states"
    id: int (PK)
    baby_id: int (FK babies.id, UNIQUE)
    active_side: str | None  # "LEFT" | "RIGHT" | null
    left_elapsed_seconds: int (default=0)
    right_elapsed_seconds: int (default=0)
    segment_start_time: datetime | None
```

From app/schemas/timer.py (Plan 02 で作成):
```python
class ContractionTimerResponse(BaseModel):
    status: Literal["idle", "timing"]
    start_time: Optional[datetime]

class ContractionTimerUpdate(BaseModel):
    status: Literal["idle", "timing"]
    start_time: Optional[datetime] = None

class FeedingTimerResponse(BaseModel):
    active_side: Optional[Literal["LEFT", "RIGHT"]]
    left_elapsed_seconds: int
    right_elapsed_seconds: int
    segment_start_time: Optional[datetime]

class FeedingTimerUpdate(BaseModel):
    active_side: Optional[Literal["LEFT", "RIGHT"]] = None
    left_elapsed_seconds: int = 0
    right_elapsed_seconds: int = 0
    segment_start_time: Optional[datetime] = None
```

From app/dependencies.py:
```python
def verify_baby_access(db, baby_id, user_id, record_type="baby", require_write=False) -> Baby:
    # 別家族は 403、対象 baby が存在しない場合も 403
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: タイマーAPIルーターを実装して main.py に登録する</name>
  <files>app/routers/timer.py, app/main.py</files>
  <action>
    **app/routers/timer.py を新規作成:**

    prefix は `/api/babies` として、baby ルーターと同じパス階層に入れる。
    両エンドポイントで `verify_baby_access(db, baby_id, current_user.id, record_type="timer")` を使い family 境界を強制する。
    タイマー状態は baby_id に対して 1 行のみ（UPSERT パターン）:
    - GET: DB に行がなければデフォルト値を返す（DB に INSERT はしない）
    - PUT: 行があれば UPDATE、なければ INSERT（get-or-create パターン）

    ```python
    from fastapi import APIRouter, Depends, HTTPException
    from sqlalchemy.orm import Session
    from app.dependencies import get_db, get_current_user, verify_baby_access
    from app.models.user import User
    from app.models.timer import ContractionTimerState, FeedingTimerState
    from app.schemas.timer import (
        ContractionTimerResponse, ContractionTimerUpdate,
        FeedingTimerResponse, FeedingTimerUpdate,
    )

    router = APIRouter(prefix="/api/babies", tags=["timer"])

    # --- 陣痛タイマー ---

    @router.get("/{baby_id}/timer/contraction", response_model=ContractionTimerResponse)
    def get_contraction_timer(
        baby_id: int,
        db: Session = Depends(get_db),
        current_user: User = Depends(get_current_user),
    ):
        verify_baby_access(db, baby_id, current_user.id, record_type="timer")
        state = db.query(ContractionTimerState).filter(
            ContractionTimerState.baby_id == baby_id
        ).first()
        if state is None:
            return ContractionTimerResponse(status="idle", start_time=None)
        return state

    @router.put("/{baby_id}/timer/contraction", response_model=ContractionTimerResponse)
    def put_contraction_timer(
        baby_id: int,
        body: ContractionTimerUpdate,
        db: Session = Depends(get_db),
        current_user: User = Depends(get_current_user),
    ):
        verify_baby_access(db, baby_id, current_user.id, record_type="timer", require_write=True)
        state = db.query(ContractionTimerState).filter(
            ContractionTimerState.baby_id == baby_id
        ).first()
        if state is None:
            state = ContractionTimerState(baby_id=baby_id)
            db.add(state)
        state.status = body.status
        state.start_time = body.start_time
        db.commit()
        db.refresh(state)
        return state

    # --- 授乳タイマー ---

    @router.get("/{baby_id}/timer/feeding", response_model=FeedingTimerResponse)
    def get_feeding_timer(
        baby_id: int,
        db: Session = Depends(get_db),
        current_user: User = Depends(get_current_user),
    ):
        verify_baby_access(db, baby_id, current_user.id, record_type="timer")
        state = db.query(FeedingTimerState).filter(
            FeedingTimerState.baby_id == baby_id
        ).first()
        if state is None:
            return FeedingTimerResponse(
                active_side=None,
                left_elapsed_seconds=0,
                right_elapsed_seconds=0,
                segment_start_time=None,
            )
        return state

    @router.put("/{baby_id}/timer/feeding", response_model=FeedingTimerResponse)
    def put_feeding_timer(
        baby_id: int,
        body: FeedingTimerUpdate,
        db: Session = Depends(get_db),
        current_user: User = Depends(get_current_user),
    ):
        verify_baby_access(db, baby_id, current_user.id, record_type="timer", require_write=True)
        state = db.query(FeedingTimerState).filter(
            FeedingTimerState.baby_id == baby_id
        ).first()
        if state is None:
            state = FeedingTimerState(baby_id=baby_id)
            db.add(state)
        state.active_side = body.active_side
        state.left_elapsed_seconds = body.left_elapsed_seconds
        state.right_elapsed_seconds = body.right_elapsed_seconds
        state.segment_start_time = body.segment_start_time
        db.commit()
        db.refresh(state)
        return state
    ```

    **app/main.py を更新:**
    既存の `from app.routers import ...` ブロックの後に追加:
    ```python
    from app.routers import timer
    ```
    `app.include_router(comments.router)` の行の後に追加:
    ```python
    app.include_router(timer.router)
    ```

    **注意:** verify_baby_access の record_type="timer" は BabyPermission テーブルに "timer" エントリが存在しないため、
    MEMBER/VIEWER のデフォルト拒否ロジックに引っかかる可能性がある。
    dependencies.py の verify_baby_access を確認し、ADMIN ロールが timer へアクセスできることを確認する。
    テストでは auth_client がデフォルトで ADMIN ロールになる（family 作成者 = ADMIN）ため問題ない。
    しかし MEMBER ロールのユーザーも timer にアクセスできる必要があるため、
    record_type="timer" を "baby" に変更するか、または require_write=False のパスで
    BabyPermission チェックをスキップする別のアプローチを検討する。

    **推奨実装:** record_type を "timer" ではなく "baby" で verify_baby_access を呼ぶ。
    タイマー状態の閲覧は baby へのアクセス権があれば許可する（BabyPermission.record_type="baby" で判定）。
    これにより既存の ADMIN/MEMBER/VIEWER のアクセス制御が自然に適用される。

    最終的なルーター実装では `record_type="baby"` を使用すること:
    ```python
    verify_baby_access(db, baby_id, current_user.id, record_type="baby")          # GET
    verify_baby_access(db, baby_id, current_user.id, record_type="baby", require_write=True)  # PUT
    ```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby && python -m pytest tests/test_timer_contraction.py tests/test_timer_feeding.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - app/routers/timer.py が存在し 4 つのエンドポイントを実装している
    - app/main.py が timer.router をインクルードしている
    - tests/test_timer_contraction.py の全テストが PASSED
    - tests/test_timer_feeding.py の全テストが PASSED
  </done>
</task>

<task type="auto">
  <name>Task 2: openapi.json を更新する</name>
  <files>frontend/openapi.json</files>
  <action>
    バックエンドルーターに timer エンドポイントを追加したので、openapi.json を再生成する:

    ```bash
    source .venv/bin/activate && set -a && source .env && set +a
    python scripts/export_openapi.py
    git add frontend/openapi.json
    ```

    CLAUDE.md の指示: バックエンド変更後は openapi.json を必ず更新する。
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby && python -c "import json; d=json.load(open('frontend/openapi.json')); paths=list(d['paths'].keys()); print([p for p in paths if 'timer' in p])"</automated>
  </verify>
  <done>
    - frontend/openapi.json に /api/babies/{baby_id}/timer/contraction と /api/babies/{baby_id}/timer/feeding のパスが含まれている
    - git add 済み
  </done>
</task>

</tasks>

<verification>
```bash
# 全テストが GREEN
cd /Users/ry1e/Documents/work/baby && python -m pytest tests/test_timer_contraction.py tests/test_timer_feeding.py -v

# openapi.json にタイマーパスが含まれている
python -c "import json; d=json.load(open('frontend/openapi.json')); print([p for p in d['paths'] if 'timer' in p])"

# 既存テストに回帰がないことを確認
python -m pytest tests/ -x --timeout=60 -q 2>&1 | tail -20
```
</verification>

<success_criteria>
1. GET /api/babies/{baby_id}/timer/contraction が {"status": "idle", "start_time": null} を返す（初期状態）
2. PUT → GET で陣痛タイマー状態が永続化されている
3. GET /api/babies/{baby_id}/timer/feeding が {"active_side": null, "left_elapsed_seconds": 0, ...} を返す（初期状態）
4. PUT → GET で授乳タイマー状態が永続化されている
5. 別家族ユーザーによるアクセスが 403 を返す
6. tests/test_timer_contraction.py と tests/test_timer_feeding.py の全テストが GREEN
7. frontend/openapi.json にタイマーエンドポイントが反映されている
</success_criteria>

<output>
After completion, create `.planning/phases/08-backend-timer-api/08-03-SUMMARY.md`
</output>
