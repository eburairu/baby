---
phase: 04-baby-widget
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/app/(dashboard)/dashboard/page.tsx
  - frontend/constants/dashboard.ts
autonomous: true
requirements:
  - WIDGET-01
  - CLEANUP-01

must_haves:
  truths:
    - "ハニカムグリッド中段中央に BabyWidget が表示される"
    - "ダッシュボード上部に BabyProfileCard が存在しない"
    - "情報の重複がない（BabyWidget と BabyProfileCard が同時に表示されない）"
  artifacts:
    - path: "frontend/app/(dashboard)/dashboard/page.tsx"
      provides: "ダッシュボードページ（BabyWidget 組み込み済み、BabyProfileCard 削除済み）"
    - path: "frontend/constants/dashboard.ts"
      provides: "WIDGET_ROWS 更新済み（null → 6 に変更）"
  key_links:
    - from: "frontend/app/(dashboard)/dashboard/page.tsx"
      to: "frontend/components/dashboard/BabyWidget.tsx"
      via: "HoneycombGrid の子要素として組み込み"
      pattern: "BabyWidget"
    - from: "frontend/constants/dashboard.ts"
      to: "frontend/components/ui/honeycomb-grid.tsx"
      via: "WIDGET_ROWS の null スロットを index 6 に変更"
      pattern: "WIDGET_ROWS"
---

<objective>
BabyWidget をダッシュボードのハニカムグリッド中央スロットに組み込み、BabyProfileCard を削除する。

Purpose: WIDGET-01（グリッド中央配置）と CLEANUP-01（BabyProfileCard 廃止）を満たし、情報の一本化を達成する。
Output: BabyWidget が中段中央に表示され、BabyProfileCard が存在しないダッシュボード。
</objective>

<execution_context>
@/Users/ry1e/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ry1e/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-baby-widget/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01 で作成されたコンポーネント -->

From frontend/components/dashboard/BabyWidget.tsx (Plan 01 で作成):
```typescript
interface BabyForWidget {
  id: number
  name: string
  birthday?: string | null
  due_date?: string | null
  gender?: ("boy" | "girl" | "unknown") | null
  characteristics?: string | null
}

// named export
export function BabyWidget({ baby, size }: { baby: BabyForWidget, size?: number }): JSX.Element
```

From frontend/components/ui/honeycomb-grid.tsx:
```typescript
// rows: ReadonlyArray<ReadonlyArray<number | null>>
// null は空スペーサー（子要素を消費しない）
// childrenArray[index] の順番で子要素を割り当て
// 現在の WIDGET_ROWS: [[0, 2], [1, null, 3], [4, 5]]
// BabyWidget を中段中央（null の位置）に入れるには:
//   WIDGET_ROWS を [[0, 2], [1, 6, 3], [4, 5]] に変更し
//   HoneycombGrid の 7 番目の子要素（index 6）として BabyWidget を追加
```

From frontend/app/(dashboard)/dashboard/page.tsx (現在の構造):
```typescript
// 削除対象: BabyProfileCard のインポートと JSX
import { BabyProfileCard } from "@/components/dashboard/BabyProfileCard"
// <BabyProfileCard key={effectiveBabyId} babies={babiesWithStrId} selectedBabyId={effectiveBabyId} />

// 追加対象: BabyWidget のインポートと HoneycombGrid 内への配置
// HoneycombGrid の子要素の現在の順序:
//   0: FeedingWidget, 1: SleepWidget, 2: DiaperWidget,
//   3: GrowthWidget, 4: NoteWidget, 5: DiaryWidget
// BabyWidget を index 6 として末尾に追加

// selectedBaby は既に page.tsx で使用可能:
const selectedBaby = babies.find(b => String(b.id) === effectiveBabyId)
// selectedBaby は BabyResponse 型（id: number を持つ）
```

From frontend/constants/dashboard.ts (変更箇所):
```typescript
// 変更前:
WIDGET_ROWS: [[0, 2], [1, null, 3], [4, 5]],
// 変更後:
WIDGET_ROWS: [[0, 2], [1, 6, 3], [4, 5]],
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WIDGET_ROWS 更新と dashboard.tsx への BabyWidget 組み込み</name>
  <files>
    frontend/constants/dashboard.ts
    frontend/app/(dashboard)/dashboard/page.tsx
  </files>
  <action>
**Step 1: `frontend/constants/dashboard.ts` の WIDGET_ROWS を更新する**

`null` を `6` に変更して BabyWidget の配置スロットを確保する:

```typescript
WIDGET_ROWS: [[0, 2], [1, 6, 3], [4, 5]],
```

コメントも更新:
```typescript
/**
 * 0: Feeding, 1: Sleep, 2: Diaper, 3: Growth, 4: Note, 5: Diary, 6: BabyInfo
 */
```

**Step 2: `frontend/app/(dashboard)/dashboard/page.tsx` を修正する**

変更内容:
1. `BabyProfileCard` のインポートを削除する（ファイル先頭の import 文）
2. JSX 内の `<BabyProfileCard .../>` を削除する（`<main>` 直下にある）
3. `BabyWidget` のインポートを追加:
   ```typescript
   import { BabyWidget } from "@/components/dashboard/BabyWidget"
   ```
4. `HoneycombGrid` の末尾の子要素として `BabyWidget` を追加する。
   現在 `<DiaryWidget>` が最後の子（index 5）。その直後（`</HoneycombGrid>` の前）に:
   ```tsx
   {selectedBaby && (
     <BabyWidget
       baby={selectedBaby}
       size={honeycombSize}
     />
   )}
   ```
   selectedBaby は page.tsx の既存変数 `const selectedBaby = babies.find(...)` を使用する。

**注意点**:
- `selectedBaby` は `BabyResponse` 型（id は number）。`BabyWidget` の Props の `baby.id` も number なので型変換不要。
- `babiesWithStrId`（id を String に変換した配列）は BabyProfileCard 用だった。BabyProfileCard 削除後も `BabyBabyProfileCard` 以外で使われていないか確認し、もし不要なら変数定義ごと削除してよい。ただし他で使われていれば残す。
- `BabyProfileCard` の import を削除した後、`BabyProfileCard.tsx` ファイル自体は削除しない（plan 01 のスコープ外。CLEANUP-01 の「ダッシュボード上部から削除」は達成済み）。

コミット:
```bash
git add frontend/constants/dashboard.ts frontend/app/(dashboard)/dashboard/page.tsx
git commit -m "feat(04-02): BabyWidget をハニカムグリッド中央に配置し BabyProfileCard を削除"
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend && pnpm exec tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    - `frontend/constants/dashboard.ts` の WIDGET_ROWS が `[[0, 2], [1, 6, 3], [4, 5]]` になっている
    - `frontend/app/(dashboard)/dashboard/page.tsx` に `BabyProfileCard` のインポートが存在しない
    - `frontend/app/(dashboard)/dashboard/page.tsx` に `<BabyProfileCard` のタグが存在しない
    - `frontend/app/(dashboard)/dashboard/page.tsx` に `<BabyWidget` が存在する
    - TypeScript エラーなし
  </done>
</task>

<task type="auto">
  <name>Task 2: 全体ビルドチェックと verify_all 実行</name>
  <files></files>
  <action>
`sh scripts/verify_all.sh` を実行してすべてのチェックを通過することを確認する。

```bash
cd /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget && sh scripts/verify_all.sh
```

**エラーが出た場合の対処**:
- TypeScript エラー: 型の不一致を修正する
- ビルドエラー（`generateStaticParams` 欠如など）: 指示に従って修正する
- テスト失敗: テストを確認して修正する

修正後は同じコミットに含めるか、別コミットを作成する（内容に応じて判断）。

すべて通過したら PR 作成の準備をする（PR 作成自体はこのプランのスコープ外）。
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget && sh scripts/verify_all.sh 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `sh scripts/verify_all.sh` が全チェック Pass
    - フロントエンドビルドが成功する（`output: 'export'` の静的エクスポートとして）
    - テストが全 Pass
  </done>
</task>

</tasks>

<verification>
```bash
# BabyProfileCard がダッシュボードから除去されていること
grep -n "BabyProfileCard" /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend/app/(dashboard)/dashboard/page.tsx || echo "BabyProfileCard なし - OK"

# BabyWidget がダッシュボードに追加されていること
grep -n "BabyWidget" /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend/app/(dashboard)/dashboard/page.tsx

# WIDGET_ROWS の null が 6 に変更されていること
grep "WIDGET_ROWS" /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend/constants/dashboard.ts

# 全体ビルドと全テストが通ること
cd /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget && sh scripts/verify_all.sh 2>&1 | tail -5
```
</verification>

<success_criteria>
- ダッシュボードの HoneycombGrid の 7 番目の子要素として BabyWidget が配置されている
- WIDGET_ROWS が `[[0, 2], [1, 6, 3], [4, 5]]` に更新されている
- ダッシュボードページに BabyProfileCard のインポートも JSX も存在しない
- `sh scripts/verify_all.sh` が全チェック Pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-baby-widget/04-02-SUMMARY.md`
</output>
