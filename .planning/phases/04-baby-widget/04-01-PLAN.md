---
phase: 04-baby-widget
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/__tests__/babyWidget.test.tsx
  - frontend/components/dashboard/BabyWidget.tsx
  - frontend/components/dashboard/BabyInfoPopup.tsx
autonomous: true
requirements:
  - WIDGET-01
  - WIDGET-02
  - WIDGET-03
  - WIDGET-04

must_haves:
  truths:
    - "BabyWidget はイニシャル・名前・月齢を六角形内に表示する"
    - "BabyWidget をタップすると BabyInfoPopup が開く"
    - "BabyInfoPopup は名前・誕生日・月齢・性別・characteristics を表示する"
    - "BabyInfoPopup に赤ちゃんプロフィール編集ページへのリンクが存在する"
  artifacts:
    - path: "frontend/__tests__/babyWidget.test.tsx"
      provides: "BabyWidget と BabyInfoPopup のレンダリングロジックテスト"
    - path: "frontend/components/dashboard/BabyWidget.tsx"
      provides: "ハニカムグリッド用赤ちゃん情報ウィジェット"
      exports: ["BabyWidget"]
    - path: "frontend/components/dashboard/BabyInfoPopup.tsx"
      provides: "赤ちゃん詳細情報ポップアップ（Sheet ベース）"
      exports: ["BabyInfoPopup"]
  key_links:
    - from: "frontend/components/dashboard/BabyWidget.tsx"
      to: "frontend/components/dashboard/BabyInfoPopup.tsx"
      via: "onClick で open state を切り替え"
      pattern: "BabyInfoPopup"
    - from: "frontend/components/dashboard/BabyInfoPopup.tsx"
      to: "/babies/[id]/edit"
      via: "Link コンポーネント"
      pattern: "href.*babies.*edit"
---

<objective>
BabyWidget と BabyInfoPopup コンポーネントを TDD で実装する。

Purpose: テストを先に書いて仕様を確定させ、実装漏れなく WIDGET-01〜WIDGET-04 を満たす。
Output: テスト済みの BabyWidget（六角形ウィジェット）と BabyInfoPopup（詳細シート）コンポーネント。
</objective>

<execution_context>
@/Users/ry1e/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ry1e/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- 実装に必要な既存インターフェース -->

From frontend/types/generated/api.d.ts:
```typescript
BabyResponse: {
  birthday?: string | null;
  characteristics?: string | null;
  created_at: string;
  due_date?: string | null;
  family_id: number;
  gender?: ("boy" | "girl" | "unknown") | null;
  id: number;
  name: string;
}
```

From frontend/components/dashboard/HexagonWidgetCard.tsx:
```typescript
interface HexagonWidgetCardProps {
  title: string
  icon?: React.ReactNode
  children?: React.ReactNode
  isError?: boolean | ErrorWithStatus | null | unknown
  isLoading?: boolean
  className?: string
  size?: number  // default 160
}
export const HexagonWidgetCard: React.FC<HexagonWidgetCardProps>
```

From frontend/lib/ageUtils.ts:
```typescript
export function calcAge(birthday: string): { months: number; days: number; label: string }
```

From frontend/lib/babyUtils.ts:
```typescript
export function getPrenatalLabel(dueDate?: string | null): string
```

From frontend/components/ui/sheet.tsx:
```typescript
// radix-ui Dialog ベースのシートコンポーネント
export { Sheet, SheetTrigger, SheetClose, SheetContent, SheetHeader,
         SheetFooter, SheetTitle, SheetDescription, SheetPortal, SheetOverlay }
// SheetContent の side prop: "top" | "bottom" | "left" | "right"
// モバイルフレンドリーにするため side="bottom" を使用
```

From frontend/components/ui/hexagon.tsx:
```typescript
export const Hexagon: React.FC<{
  children?: React.ReactNode
  size?: number
  color?: string
  borderColor?: string
  borderWidth?: number
  className?: string
  // ...others
}>
```

From frontend/vitest.config.ts:
```typescript
// environment: 'jsdom'
// alias: { '@': path.resolve(__dirname, './') }
// テストは frontend/__tests__/ に配置
// import { describe, it, expect, vi } from "vitest"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: テスト作成（Red）</name>
  <files>frontend/__tests__/babyWidget.test.tsx</files>
  <action>
作業ディレクトリは `worktrees/feat/v1.1-baby-widget/` 内であることを確認する（`git branch --show-current` で `feat/v1.1-baby-widget` であれば OK）。

`frontend/__tests__/babyWidget.test.tsx` を作成する。
@testing-library/react は package.json に含まれないため、コンポーネントの DOM テストではなく、
BabyWidget が依存するロジック（イニシャル計算・性別ラベル変換・誕生日フォーマット）のユニットテストを書く。

```typescript
import { describe, it, expect } from "vitest"
import { calcAge } from "@/lib/ageUtils"

describe("BabyWidget の表示ロジック", () => {
  describe("イニシャル計算", () => {
    it("名前の最初の文字をイニシャルとして返す", () => {
      expect("太郎".charAt(0) || "?").toBe("太")
    })
    it("名前が空の場合は '?' を返す", () => {
      expect("".charAt(0) || "?").toBe("?")
    })
    it("英語名の場合も最初の文字を返す", () => {
      expect("Alice".charAt(0) || "?").toBe("A")
    })
  })

  describe("月齢ラベル（calcAge 経由）", () => {
    it("誕生日があれば月齢ラベルが '生後' で始まる", () => {
      const result = calcAge("2024-01-01")
      expect(result.label).toMatch(/^生後/)
    })
  })

  describe("性別ラベル変換", () => {
    const genderLabel = (g: string | null | undefined) =>
      g === "boy" ? "男の子" : g === "girl" ? "女の子" : "不明"

    it("'boy' は '男の子' に変換される", () => {
      expect(genderLabel("boy")).toBe("男の子")
    })
    it("'girl' は '女の子' に変換される", () => {
      expect(genderLabel("girl")).toBe("女の子")
    })
    it("'unknown' は '不明' に変換される", () => {
      expect(genderLabel("unknown")).toBe("不明")
    })
    it("null は '不明' に変換される", () => {
      expect(genderLabel(null)).toBe("不明")
    })
    it("undefined は '不明' に変換される", () => {
      expect(genderLabel(undefined)).toBe("不明")
    })
  })

  describe("誕生日フォーマット", () => {
    const formatBirthday = (birthday: string) => {
      const d = new Date(birthday)
      return `${d.getFullYear()}年${d.getMonth() + 1}月${d.getDate()}日`
    }

    it("'2024-03-15' を '2024年3月15日' に変換できる", () => {
      expect(formatBirthday("2024-03-15")).toBe("2024年3月15日")
    })
    it("'2025-01-01' を '2025年1月1日' に変換できる", () => {
      expect(formatBirthday("2025-01-01")).toBe("2025年1月1日")
    })
  })
})
```

テストを実行して全 Pass を確認:
```bash
cd frontend && pnpm test -- __tests__/babyWidget.test.tsx
```

コミット:
```bash
git add frontend/__tests__/babyWidget.test.tsx
git commit -m "test(04-01): BabyWidget の表示ロジックに関するテストを追加（Red）"
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend && pnpm test -- __tests__/babyWidget.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `frontend/__tests__/babyWidget.test.tsx` が存在する
    - `pnpm test -- __tests__/babyWidget.test.tsx` で全テストが Pass
  </done>
</task>

<task type="auto">
  <name>Task 2: BabyWidget と BabyInfoPopup の実装（Green）</name>
  <files>
    frontend/components/dashboard/BabyWidget.tsx
    frontend/components/dashboard/BabyInfoPopup.tsx
  </files>
  <action>
**BabyInfoPopup.tsx を先に作成する**（BabyWidget がインポートするため）。

`frontend/components/dashboard/BabyInfoPopup.tsx`:
- `"use client"` 宣言必須（useState を使う親 BabyWidget の影響で不要だが、Sheet は client component）
- Props: `{ baby: BabyForWidget, open: boolean, onOpenChange: (open: boolean) => void }`
- `Sheet` + `SheetContent side="bottom"` を使用（`frontend/components/ui/sheet.tsx` から import）
- `SheetContent className="max-h-[85vh] rounded-t-[10px] overflow-y-auto"`
- SheetHeader: 
  - Hexagon（size=56、color="var(--primary)"）内にイニシャル文字
  - SheetTitle に baby.name（大見出し）
  - SheetDescription に月齢ラベル（birthday あり→calcAge.label、なし→getPrenatalLabel）
- 情報行（label + value のシンプルな行）:
  - 誕生日: birthday あり→ `YYYY年M月D日` 形式、なし→ `"未登録"`
  - 性別: `boy→男の子 / girl→女の子 / その他→不明`
- 特徴（characteristics）: あれば Sparkles アイコン付きで表示、なければ表示しない
- 編集リンク:
  ```tsx
  <SheetClose asChild>
    <Link href={`/babies/${baby.id}/edit`} className="...">
      <Edit className="w-4 h-4" />
      プロフィールを編集
    </Link>
  </SheetClose>
  ```
  スタイル: `flex items-center justify-center gap-2 w-full py-2.5 px-4 rounded-xl bg-indigo-50 dark:bg-indigo-950/30 text-indigo-600 dark:text-indigo-400 font-medium text-sm`

**BabyWidget.tsx を作成する**:

`frontend/components/dashboard/BabyWidget.tsx`:
- `"use client"` 宣言必須
- Props: `{ baby: BabyForWidget, size?: number }`
- BabyForWidget 型を同ファイル内で定義:
  ```typescript
  interface BabyForWidget {
    id: number
    name: string
    birthday?: string | null
    due_date?: string | null
    gender?: ("boy" | "girl" | "unknown") | null
    characteristics?: string | null
  }
  ```
- `useState<boolean>(false)` で `open` を管理
- イニシャル: `baby.name.charAt(0) || "?"`
- 月齢ラベル: `baby.birthday ? calcAge(baby.birthday).label : getPrenatalLabel(baby.due_date)`
- HexagonWidgetCard を `<button>` でラップしてクリックで `setOpen(true)`:
  - `title={baby.name}`
  - `icon`: Hexagon（size=28、color="rgb(99 102 241)"）内にイニシャル
  - `children`: 月齢ラベルの `<span>`
  - `size={size}`
  - `className="hover:shadow-indigo-100 dark:hover:shadow-indigo-900/20"`
- `<BabyInfoPopup baby={baby} open={open} onOpenChange={setOpen} />` をレンダリング
- button に `aria-label={`${baby.name}の情報を見る`}` を付与

コミット:
```bash
git add frontend/components/dashboard/BabyWidget.tsx frontend/components/dashboard/BabyInfoPopup.tsx
git commit -m "feat(04-01): BabyWidget と BabyInfoPopup コンポーネントを実装（Green）"
```

TypeScript チェック:
```bash
cd frontend && pnpm exec tsc --noEmit 2>&1 | head -30
```

エラーがあれば修正してから再コミット。
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend && pnpm exec tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    - `frontend/components/dashboard/BabyWidget.tsx` が存在し `BabyWidget` を named export している
    - `frontend/components/dashboard/BabyInfoPopup.tsx` が存在し `BabyInfoPopup` を named export している
    - BabyInfoPopup に `/babies/${baby.id}/edit` へのリンクが含まれている
    - `pnpm exec tsc --noEmit` がエラーなし（または 0 errors）
  </done>
</task>

</tasks>

<verification>
```bash
# テスト全パス確認
cd /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend && pnpm test -- __tests__/babyWidget.test.tsx

# TypeScript エラーなし確認
cd /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend && pnpm exec tsc --noEmit 2>&1 | head -20

# コンポーネントファイルの存在確認
ls /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend/components/dashboard/BabyWidget.tsx
ls /Users/ry1e/Documents/work/baby/worktrees/feat/v1.1-baby-widget/frontend/components/dashboard/BabyInfoPopup.tsx
```
</verification>

<success_criteria>
- `frontend/__tests__/babyWidget.test.tsx` の全テストが Pass（イニシャル・性別ラベル・誕生日フォーマット）
- `BabyWidget.tsx`: HexagonWidgetCard を使用し、イニシャル・名前・月齢を表示、クリックで BabyInfoPopup を開く
- `BabyInfoPopup.tsx`: Sheet（bottom）で名前・誕生日・月齢・性別・特徴を表示し、`/babies/{id}/edit` へのリンクを含む
- TypeScript エラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/04-baby-widget/04-01-SUMMARY.md`
</output>
