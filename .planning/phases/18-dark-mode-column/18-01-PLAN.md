---
phase: 18-dark-mode-column
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/lib/column-data.ts, frontend/components/landing/LandingColumnTeaser.tsx]
autonomous: true
requirements: [UI-DARK-01]
must_haves:
  truths:
    - "LandingColumnTeaser components use centralized category colors"
    - "Category badges have visible contrast in both light and dark modes"
  artifacts:
    - path: "frontend/lib/column-data.ts"
      provides: "Centralized CATEGORY_COLORS with dark mode support"
    - path: "frontend/components/landing/LandingColumnTeaser.tsx"
      provides: "Dark mode optimized LP teaser section"
  key_links:
    - from: "frontend/components/landing/LandingColumnTeaser.tsx"
      to: "frontend/lib/column-data.ts"
      via: "import CATEGORY_COLORS"
---

<objective>
Refactor category colors into a centralized constant and apply dark mode support to the LandingColumnTeaser component.

Purpose: Reduce duplication and ensure consistent dark mode appearance for column categories across the site.
Output: Centralized color constants and updated LP component.
</objective>

<execution_context>
@/Users/ry1e/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/lib/column-data.ts
@frontend/components/landing/LandingColumnTeaser.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize CATEGORY_COLORS in column-data.ts</name>
  <files>frontend/lib/column-data.ts</files>
  <action>
    Export a `CATEGORY_COLORS` constant from `frontend/lib/column-data.ts`.
    Include dark mode classes for each category:
    - "授乳・ミルク": "bg-rose-50 text-rose-600 dark:bg-rose-900/20 dark:text-rose-400"
    - "睡眠": "bg-indigo-50 text-indigo-600 dark:bg-indigo-900/20 dark:text-indigo-400"
    - "おむつ・健康": "bg-amber-50 text-amber-600 dark:bg-amber-900/20 dark:text-amber-400"
    - "夫婦・家族": "bg-emerald-50 text-emerald-600 dark:bg-emerald-900/20 dark:text-emerald-400"
    - "成長記録": "bg-blue-50 text-blue-600 dark:bg-blue-900/20 dark:text-blue-400"
  </action>
  <verify>grep "CATEGORY_COLORS" frontend/lib/column-data.ts</verify>
  <done>CATEGORY_COLORS is exported with dark: classes</done>
</task>

<task type="auto">
  <name>Task 2: Update LandingColumnTeaser.tsx</name>
  <files>frontend/components/landing/LandingColumnTeaser.tsx</files>
  <action>
    - Import `CATEGORY_COLORS` from `@/lib/column-data`.
    - Remove the local `CATEGORY_COLORS` definition.
    - Ensure all elements have appropriate `dark:` classes as specified in the requirements.
    - Specifically, check text colors for titles and descriptions.
  </action>
  <verify>pnpm --prefix frontend lint</verify>
  <done>Component uses centralized colors and has full dark mode support</done>
</task>

</tasks>

<verification>
Check LandingColumnTeaser appearance in dark mode.
</verification>

<success_criteria>
- No duplicated color constants.
- LP column section looks good in dark mode.
</success_criteria>

<output>
After completion, create `.planning/phases/18-dark-mode-column/18-01-SUMMARY.md`
</output>
