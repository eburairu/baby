---
phase: 18-dark-mode-column
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: [frontend/app/column/page.tsx]
autonomous: true
requirements: [UI-DARK-01]
must_haves:
  truths:
    - "Column list page has readable text in dark mode"
    - "Article cards in the list have distinct borders in dark mode"
  artifacts:
    - path: "frontend/app/column/page.tsx"
      provides: "Dark mode optimized column list page"
---

<objective>
Update the column list page (app/column/page.tsx) to support dark mode.

Purpose: Ensure the main column index page is fully accessible and visually appealing in dark mode.
Output: Dark mode enabled column list page.
</objective>

<execution_context>
@/Users/ry1e/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/app/column/page.tsx
@frontend/lib/column-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update column index page with dark mode classes</name>
  <files>frontend/app/column/page.tsx</files>
  <action>
    - Import `CATEGORY_COLORS` from `@/lib/column-data`.
    - Remove the local `CATEGORY_COLORS` definition.
    - Add `dark:` classes to the main container, titles, descriptions, and cards.
    - Specifically:
      - `bg-slate-50` -> `dark:bg-zinc-950`
      - `bg-white` card -> `dark:bg-zinc-900`, `dark:border-zinc-800`
      - `text-slate-900` title -> `dark:text-slate-100`
      - `text-slate-600` / `text-slate-500` -> `dark:text-slate-400`
      - Accent CTA block (`bg-indigo-50`) -> `dark:bg-indigo-900/20`, `dark:border-indigo-900/30`
      - Text in CTA block -> `dark:text-slate-100`, `dark:text-slate-400`
  </action>
  <verify>pnpm --prefix frontend lint</verify>
  <done>Column index page has full dark mode support</done>
</task>

</tasks>

<verification>
Check column index page in dark mode.
</verification>

<success_criteria>
- Entire page respects dark mode settings.
- Contrast ratios meet accessibility standards for dark mode.
</success_criteria>

<output>
After completion, create `.planning/phases/18-dark-mode-column/18-02-SUMMARY.md`
</output>
