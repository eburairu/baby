---
phase: 05-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_baby_thresholds.py
autonomous: true
requirements:
  - THRES-03
must_haves:
  truths:
    - tests/test_baby_thresholds.py が存在し、5つのテスト関数を含む
    - pytest を実行すると全テストが FAILED / ERROR になる（RED フェーズ）
    - テストはバックエンドの /api/babies/ と /api/babies/{id} エンドポイントを使用している
  artifacts:
    - tests/test_baby_thresholds.py
  key_links:
    - tests/test_baby_thresholds.py が tests/conftest.py の auth_client フィクスチャを使用している
---

<objective>
Phase 5（バックエンド閾値API）のTDDフェーズ1: テストを先に書いてREDにする。

Purpose: CLAUDE.mdのTDDルールに従い、実装前にテストを作成して受け入れ条件を確認する。
Output: tests/test_baby_thresholds.py（全テストがRED状態）
</objective>

<execution_context>
@/Users/ry1e/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ry1e/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 既存コードから抽出したインターフェース。エグゼキューターはこれをそのまま使う。 -->

From app/schemas/baby.py（現在の状態 — 閾値フィールドはまだ存在しない）:
```python
class BabyBase(BaseModel):
    name: str = Field(..., max_length=100)
    birthday: Optional[date] = None
    due_date: Optional[date] = None
    gender: Optional[Literal["boy", "girl", "unknown"]] = None
    characteristics: Optional[str] = Field(None, max_length=1000)

class BabyCreate(BabyBase):
    pass

class BabyUpdate(BaseModel):
    name: Optional[str] = Field(None, max_length=100)
    birthday: Optional[date] = None
    due_date: Optional[date] = None
    gender: Optional[Literal["boy", "girl", "unknown"]] = None
    characteristics: Optional[str] = Field(None, max_length=1000)

class BabyResponse(BabyBase):
    id: int
    family_id: int
    created_at: datetime
    model_config = ConfigDict(from_attributes=True)
```

From tests/conftest.py（auth_client フィクスチャ）:
```python
@pytest.fixture
def auth_client(client):
    def _auth(username="testuser", password="testpassword123", family_name="Test Family"):
        # POST /api/auth/register/family で家族+管理者ユーザー登録
        # POST /api/auth/login でログイン
        # cookieつきclientを返す
        ...
    return _auth
```

From tests/test_baby_characteristics.py（既存テストのパターン）:
```python
def test_create_baby_with_characteristics(auth_client):
    client = auth_client()
    res = client.post("/api/babies/", json={"name": "CharBaby", "birthday": "2025-01-01", "characteristics": "Initial text"})
    assert res.status_code == 200
    baby_id = res.json()["id"]
    res = client.patch(f"/api/babies/{baby_id}", json={"characteristics": "Updated text"})
    assert res.status_code == 200
    assert res.json()["characteristics"] == "Updated text"
```

From app/models/baby.py（現在のBabyモデル — 閾値カラムはまだない）:
```python
class Baby(Base):
    __tablename__ = "babies"
    id = Column(Integer, primary_key=True, ...)
    family_id = Column(Integer, ForeignKey("families.id"), ...)
    name = Column(String, nullable=False)
    birthday = Column(Date, nullable=True)
    created_at = Column(DateTime, ...)
    due_date = Column(Date, nullable=True)
    gender = Column(String, nullable=True)
    characteristics = Column(String, nullable=True)
```

From app/services/baby.py（update_baby は任意フィールドを受け入れる — 変更不要）:
```python
def update_baby(db: Session, baby: Baby, update_data: Union[BabyUpdate, Dict[str, Any]]) -> Baby:
    if isinstance(update_data, BabyUpdate):
        update_data = update_data.model_dump(exclude_unset=True)
    for key, value in update_data.items():
        if hasattr(baby, key):
            setattr(baby, key, value)
    db.commit()
    db.refresh(baby)
    return baby
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 閾値フィールドのテスト作成（TDD RED フェーズ）</name>
  <files>tests/test_baby_thresholds.py</files>
  <action>
以下の5つのテスト関数を含む tests/test_baby_thresholds.py を新規作成する。

**テスト設計の要件（フェーズ5の受け入れ条件に対応）:**

1. `test_threshold_null_by_default`
   - 閾値を指定せずBabyを作成したとき、`feeding_threshold_minutes` と `diaper_threshold_minutes` が null であることを確認
   - GET /api/babies/ のレスポンスに両フィールドが含まれることを確認

2. `test_baby_response_includes_threshold_fields`
   - POST /api/babies/ のレスポンスに `feeding_threshold_minutes` と `diaper_threshold_minutes` フィールドが含まれることを確認

3. `test_create_baby_with_thresholds`
   - POST /api/babies/ で `feeding_threshold_minutes=180` と `diaper_threshold_minutes=240` を指定してBabyを作成
   - レスポンスに指定した値が返ることを確認

4. `test_update_baby_thresholds`
   - Babyを作成後、PATCH /api/babies/{baby_id} で `feeding_threshold_minutes=120` に更新
   - GET /api/babies/ で 120 が返ることを確認
   - さらに PATCH で `diaper_threshold_minutes=300` に更新
   - GET で 300 が返り、`feeding_threshold_minutes` が 120 のまま変わっていないことを確認（部分更新）

5. `test_threshold_family_shared`
   - 管理者ユーザーが Baby を作成し、PATCH で `feeding_threshold_minutes=150` に設定
   - 同一クライアントで GET /api/babies/ を再度呼び、`feeding_threshold_minutes=150` が返ることを確認
   - Note: 同一家族の別ユーザーによる検証はフロントエンド統合テストで行う。バックエンドはDB直接保存を保証すれば十分。
     コメントに `# THRES-03: Family sharing is verified at DB level; cross-user test in Phase 7 integration` と記載すること。

**全テストが RED になることが前提。**
現時点では `feeding_threshold_minutes` が BabyResponse に存在しないため、
`assert "feeding_threshold_minutes" in data` が AssertionError で落ちる。
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/plan/phase-5-api-thresholds && source .venv/bin/activate && python -m pytest tests/test_baby_thresholds.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
- tests/test_baby_thresholds.py が存在し、5つのテスト関数が定義されている
- pytest 実行で全テストが FAILED または ERROR になる（GREEN ではない）
- エラーが閾値フィールドの不在（KeyError / AssertionError）に起因している
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/ry1e/Documents/work/baby/worktrees/plan/phase-5-api-thresholds
source .venv/bin/activate
python -m pytest tests/test_baby_thresholds.py -v
```
期待結果: 全テストが FAILED / ERROR（RED フェーズ完了）
</verification>

<success_criteria>
- tests/test_baby_thresholds.py が作成された
- ファイルに5つのテスト関数が含まれる
- pytest 実行で全テストが RED（失敗）
- TDDサイクルの準備が完了し、05-02-PLAN.md の実装フェーズに進める
</success_criteria>

<output>
After completion, create `.planning/phases/05-api/05-01-SUMMARY.md`
</output>
