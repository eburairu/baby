---
phase: 10-breastfeeding-timer-polling-sync
plan: 02
type: tdd
wave: 2
depends_on: ["10-01"]
files_modified: [
  "frontend/hooks/useFeedingTimerSync.ts",
  "frontend/__tests__/hooks/useFeedingTimerSync.test.ts"
]
autonomous: true
requirements: [TIMER-FE-03, TIMER-FE-05]

must_haves:
  truths:
    - "3秒間隔でバックエンドのタイマー状態（GET /api/babies/{id}/timer/feeding）をポーリングする"
    - "ポーリング結果を `feedingTimerStore.sync` に渡してストアの状態を更新する"
    - "コンポーネントがマウントされた際に初回データ取得と同期を行う"
  artifacts:
    - path: "frontend/hooks/useFeedingTimerSync.ts"
      provides: "タイマー状態のポーリングと同期トリガー"
    - path: "frontend/__tests__/hooks/useFeedingTimerSync.test.ts"
      provides: "同期フックの動作検証（APIモック）"
  key_links:
    - from: "frontend/hooks/useFeedingTimerSync.ts"
      to: "frontend/stores/feedingTimerStore.ts"
      via: "sync 呼び出し"
    - from: "frontend/hooks/useFeedingTimerSync.ts"
      to: "useSWR"
      via: "pollingInterval: 3000"
---

<objective>
バックエンドのタイマー状態を定期的に取得し、Zustand ストアに反映するための `useFeedingTimerSync` フックを実装する。

目的: 別デバイスからの操作を準リアルタイム（3秒以内）で反映させる。
出力: `frontend/hooks/useFeedingTimerSync.ts`, `frontend/__tests__/hooks/useFeedingTimerSync.test.ts`
</objective>

<execution_context>
@/Users/ry1e/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/hooks/useContractionTimerSync.ts
@frontend/stores/feedingTimerStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 授乳タイマー同期フックのテスト作成 (TDD RED)</name>
  <files>frontend/__tests__/hooks/useFeedingTimerSync.test.ts</files>
  <action>
    `useFeedingTimerSync` のテストを作成する。
    - MSW または `vi.mock` を使用して、`GET /api/babies/{id}/timer/feeding` のレスポンスをモックする。
    - フックが呼ばれた際、初回取得が行われ、`feedingTimerStore.sync` が呼ばれることを確認。
    - 3秒経過後、再度リクエストが発生し、更新されたデータで同期されることを確認。
  </action>
  <verify>npm test frontend/__tests__/hooks/useFeedingTimerSync.test.ts が失敗することを確認（RED）</verify>
  <done>テストファイルが作成され、期待通り失敗する</done>
</task>

<task type="auto">
  <name>Task 2: 授乳タイマー同期フックの実装 (TDD GREEN)</name>
  <files>frontend/hooks/useFeedingTimerSync.ts</files>
  <action>
    `useSWR` を利用して 3 秒間隔のポーリングを実装する。
    - エンドポイント: `/api/babies/{id}/timer/feeding`
    - 設定: `refreshInterval: 3000`
    - `onSuccess` コールバック等を使用して、取得したデータを `useFeedingTimerStore.getState().sync` に渡す。
    - `useContractionTimerSync.ts` の実装パターンを参考にする。
  </action>
  <verify>npm test frontend/__tests__/hooks/useFeedingTimerSync.test.ts がパスすることを確認（GREEN）</verify>
  <done>同期フックが実装され、テストが全てパスする</done>
</task>

</tasks>

<verification>
- 同期フックのテストが全てパスすること。
- モックされた API レスポンスが 3 秒間隔でストアに反映されていること。
</verification>

<success_criteria>
- フックがマウントされるとポーリングが開始される。
- サーバー側の状態変更が 3 秒以内にストアへ伝播する。
</success_criteria>

<output>
After completion, create `.planning/phases/10-breastfeeding-timer-polling-sync/10-02-SUMMARY.md`
</output>
