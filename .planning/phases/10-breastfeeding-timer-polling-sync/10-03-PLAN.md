---
phase: 10-breastfeeding-timer-polling-sync
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: [
  "frontend/hooks/useFeedingTimer.ts",
  "frontend/components/feeding/feeding-form.tsx"
]
autonomous: false
requirements: [TIMER-FE-03, TIMER-FE-04, TIMER-FE-05]

must_haves:
  truths:
    - "授乳タイマーのUIが Zustand ストアと連動し、操作がバックエンドに即座に送信される"
    - "バックグラウンドでポーリングが行われ、他デバイスの操作がUIに反映される"
    - "タイマーの「跳ね（ジャンプ）」が1-2秒以内に抑えられている"
  artifacts:
    - path: "frontend/hooks/useFeedingTimer.ts"
      provides: "Zustand ストアを利用したUI用ロジック"
    - path: "frontend/components/feeding/feeding-form.tsx"
      provides: "同期フックの統合"
  key_links:
    - from: "frontend/hooks/useFeedingTimer.ts"
      to: "frontend/stores/feedingTimerStore.ts"
      via: "ストア参照"
    - from: "frontend/components/feeding/feeding-form.tsx"
      to: "frontend/hooks/useFeedingTimerSync.ts"
      via: "フック呼び出し"
---

<objective>
既存の `useFeedingTimer` を Zustand ストア依存にリファクタリングし、`FeedingForm` に同期機能を組み込む。
手動検証により、デバイス間での 3 秒以内の同期を最終確認する。

目的: ユーザー体験としてリアルタイム同期を完成させる。
出力: `frontend/hooks/useFeedingTimer.ts` (リファクタ), `frontend/components/feeding/feeding-form.tsx` (統合)
</objective>

<execution_context>
@/Users/ry1e/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/hooks/useFeedingTimer.ts
@frontend/components/feeding/feeding-form.tsx
@frontend/hooks/useFeedingTimerSync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useFeedingTimer のリファクタリング</name>
  <files>frontend/hooks/useFeedingTimer.ts</files>
  <action>
    `useFeedingTimer` 内の `useState` や `useRef` を廃止し、`useFeedingTimerStore` への参照に置き換える。
    - `leftSeconds`, `rightSeconds`, `activeBreastSide` 等をストアから取得する。
    - `toggleTimer`, `resetAllTimers` 等のアクション内でストアのメソッドを呼び出す。
    - **重要**: タイマー操作（開始・停止・切り替え）が行われるたびに、`PUT /api/babies/{id}/timer/feeding` を呼び出してサーバーに状態を即座に書き込む。
  </action>
  <verify>
    - `npm test frontend/hooks/useFeedingTimer.ts` (もしあれば)
    - ビルドエラーが出ないこと
  </verify>
  <done>フックがストア依存になり、操作時にサーバー同期が行われる</done>
</task>

<task type="auto">
  <name>Task 2: FeedingForm への同期フック統合</name>
  <files>frontend/components/feeding/feeding-form.tsx</files>
  <action>
    `FeedingForm` コンポーネントで `useFeedingTimerSync(babyId)` を呼び出す。
    - これにより、フォームが表示されている間、3秒間隔のバックグラウンド同期が有効になる。
  </action>
  <verify>ブラウザで授乳ページを開き、ネットワークタブで 3 秒おきに GET リクエストが発生していることを確認</verify>
  <done>同期フックが組み込まれ、ポーリングが開始される</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: デバイス間同期の手動検証</name>
  <what-built>Zustand ストア、同期フック、およびUI統合</what-built>
  <how-to-verify>
    1. ブラウザで2つのタブ（または2つのブラウザ/デバイス）を開き、同じ赤ちゃんの授乳記録ページにアクセスする。
    2. 片方のタブでタイマー（左）を開始する。
    3. もう片方のタブで、3秒以内にタイマーが「左計測中」に自動で切り替わることを確認する。
    4. 片方のタブでサイドを「右」に切り替える。
    5. もう片方のタブでも3秒以内に「右」に切り替わり、経過時間が整合していることを確認する。
    6. 片方のタブで停止（一時停止）し、もう片方も停止することを確認する。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- デバイスAの操作がデバイスBに3秒以内に反映される。
- タイマーの秒数が両デバイスでほぼ一致している（大きなジャンプがない）。
- 一時停止・再開を繰り返しても累積時間が壊れない。
</verification>

<success_criteria>
- 複数デバイス間での授乳タイマーの状態同期が仕様（3秒以内）通り動作する。
- ページをリロードしても、直前のタイマー状態がサーバーから復元される。
</success_criteria>

<output>
After completion, create `.planning/phases/10-breastfeeding-timer-polling-sync/10-03-SUMMARY.md`
</output>
