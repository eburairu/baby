---
phase: 10-breastfeeding-timer-polling-sync
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [
  "frontend/stores/feedingTimerStore.ts",
  "frontend/__tests__/stores/feedingTimerStore.test.ts"
]
autonomous: true
requirements: [TIMER-FE-03, TIMER-FE-04]

must_haves:
  truths:
    - "ストアが左右の計測状態（activeSide）と経過時間（leftElapsedSeconds, rightElapsedSeconds）を正しく管理できる"
    - "syncメソッドを呼び出すと、サーバーからの状態がストアに正しく反映される"
    - "tickメソッドにより、計測中のサイドの経過時間が1秒ごとに更新される"
  artifacts:
    - path: "frontend/stores/feedingTimerStore.ts"
      provides: "授乳タイマーの中央状態管理"
    - path: "frontend/__tests__/stores/feedingTimerStore.test.ts"
      provides: "ストアロジックの品質担保"
  key_links:
    - from: "frontend/stores/feedingTimerStore.ts"
      to: "Zustand"
      via: "create"
---

<objective>
授乳タイマーの中央状態管理を行う Zustand ストアを実装する。
サーバーとの同期（sync）や、フロントエンドでの経過時間のインクリメント（tick）を責務とする。

目的: 全デバイスでのタイマー状態の共通基盤を構築する。
出力: `frontend/stores/feedingTimerStore.ts`, `frontend/__tests__/stores/feedingTimerStore.test.ts`
</objective>

<execution_context>
@/Users/ry1e/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/stores/contractionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 授乳タイマーストアのテスト作成 (TDD RED)</name>
  <files>frontend/__tests__/stores/feedingTimerStore.test.ts</files>
  <action>
    授乳タイマーの状態管理と同期ロジックの期待動作を定義するテストを作成する。
    以下のケースをカバーすること：
    - 初期状態 (activeSide=null, elapsed=0)
    - 状態更新 (sync 呼び出しによる反映)
    - サイド切り替え時の動作 (左開始 -> 右開始時の経過時間引き継ぎ)
    - tick による計測中のサイドの秒数カウントアップ
    - リセット動作
  </action>
  <verify>npm test frontend/__tests__/stores/feedingTimerStore.test.ts が失敗することを確認（RED）</verify>
  <done>テストファイルが作成され、期待通り失敗する</done>
</task>

<task type="auto">
  <name>Task 2: 授乳タイマーストアの実装 (TDD GREEN)</name>
  <files>frontend/stores/feedingTimerStore.ts</files>
  <action>
    Zustand を使用して `useFeedingTimerStore` を作成する。
    - 状態: `activeSide` (LEFT|RIGHT|null), `leftElapsedSeconds`, `rightElapsedSeconds`, `segmentStartTime` (Date|null)
    - メソッド:
      - `sync(activeSide, leftElapsed, rightElapsed, segmentStartTime)`: 外部（サーバー等）からの同期。
      - `tick()`: 現在時刻に基づき、計測中のサイドの秒数を更新。
      - `start(side)`: 指定したサイドの計測を開始し、`segmentStartTime` を設定。
      - `stop()`: 計測を停止。
      - `reset()`: すべてを初期状態に戻す。
    既存の `contractionStore.ts` のパターンを参考にする。
  </action>
  <verify>npm test frontend/__tests__/stores/feedingTimerStore.test.ts がパスすることを確認（GREEN）</verify>
  <done>ストアが実装され、テストが全てパスする</done>
</task>

</tasks>

<verification>
- ストアのユニットテストが全てパスすること。
- 計測中のサイドが `tick` 呼び出しにより正しくカウントアップされること。
- `sync` 呼び出し時にサーバーの累積時間と開始時刻から、フロントエンドでの経過時間が正確に復元されること。
</verification>

<success_criteria>
- ストアの各メソッドが期待通りに状態を遷移させる。
- サーバーからの状態が `sync` によって整合性を持って反映される。
</success_criteria>

<output>
After completion, create `.planning/phases/10-breastfeeding-timer-polling-sync/10-01-SUMMARY.md`
</output>
