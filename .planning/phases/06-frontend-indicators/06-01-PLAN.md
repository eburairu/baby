---
phase: 06-frontend-indicators
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/__tests__/thresholdUtils.test.ts
  - frontend/__tests__/indicatorUtils.test.ts
autonomous: true
requirements:
  - THRES-01
  - INDIC-03
  - INDIC-04

must_haves:
  truths:
    - "thresholdUtils のテストが RED（FAIL）で実行できる（実装前にテストが存在する）"
    - "indicatorUtils のテストが RED（FAIL）で実行できる（実装前にテストが存在する）"
    - "全テストが実装ファイル不在により失敗する（モジュール未解決エラー）"
  artifacts:
    - path: "frontend/__tests__/thresholdUtils.test.ts"
      provides: "resolveThreshold のユニットテスト（月齢別閾値・カスタム閾値優先・両タイプ）"
      min_lines: 50
    - path: "frontend/__tests__/indicatorUtils.test.ts"
      provides: "calcProgress・getIndicatorColor・isOverThreshold のユニットテスト"
      min_lines: 60
  key_links:
    - from: "frontend/__tests__/thresholdUtils.test.ts"
      to: "frontend/lib/thresholdUtils.ts"
      via: "import { resolveThreshold }"
      pattern: "import.*thresholdUtils"
    - from: "frontend/__tests__/indicatorUtils.test.ts"
      to: "frontend/lib/indicatorUtils.ts"
      via: "import { calcProgress, getIndicatorColor, isOverThreshold }"
      pattern: "import.*indicatorUtils"
---

<objective>
Phase 6 の TDD RED フェーズ。thresholdUtils と indicatorUtils のテストを先に書き、実装ファイルが存在しないため全テストが FAIL することを確認してからコミットする。

Purpose: CLAUDE.md の TDD 必須ルールに従い、実装前に受け入れ条件をテストとして定義する。
Output: 2つのテストファイル（全テスト FAIL 状態）。
</objective>

<execution_context>
@/Users/ry1e/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ry1e/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- テスト対象となる予定のインターフェース。これをもとにテストを書く。実装ファイルはまだ存在しない。 -->

期待するエクスポート (frontend/lib/thresholdUtils.ts — 未作成):
```typescript
export function resolveThreshold(
  ageMonths: number,
  customMinutes: number | null | undefined,
  type: 'feeding' | 'diaper'
): number
```

期待する閾値テーブル:
- feeding: 0ヶ月=120, 1-2ヶ月=150, 3-5ヶ月=180, 6ヶ月以上=240
- diaper: 0ヶ月=180, 1-5ヶ月=240, 6ヶ月以上=360
- カスタム値（null/undefined でない正の整数）が渡された場合はカスタム値を返す

期待するエクスポート (frontend/lib/indicatorUtils.ts — 未作成):
```typescript
export const INDICATOR_COLORS: { green: string; yellow: string; red: string }

export function calcProgress(
  lastRecordTimeISO: string | null,
  thresholdMinutes: number,
  nowISO?: string
): number  // 0以上（1.0超も許容）

export function getIndicatorColor(progress: number): 'green' | 'yellow' | 'red'
// green: progress < 0.6, yellow: 0.6 <= progress < 0.8, red: progress >= 0.8

export function isOverThreshold(progress: number): boolean
// progress >= 1.0 のとき true
```

既存テストパターン (frontend/__tests__/ageUtils.test.ts):
```typescript
import { describe, it, expect, vi } from 'vitest'
// vi.setSystemTime() で時刻を固定してテスト
// afterEach(() => { vi.useRealTimers() }) でリセット
```
</interfaces>
</context>

<feature>
  <name>thresholdUtils + indicatorUtils TDD RED</name>
  <files>
    frontend/__tests__/thresholdUtils.test.ts
    frontend/__tests__/indicatorUtils.test.ts
  </files>
  <behavior>
**thresholdUtils — resolveThreshold:**
- ageMonths=0, custom=null, type='feeding' -> 120
- ageMonths=1, custom=null, type='feeding' -> 150
- ageMonths=3, custom=null, type='feeding' -> 180
- ageMonths=6, custom=null, type='feeding' -> 240
- ageMonths=12, custom=null, type='feeding' -> 240 (6ヶ月以上は全て同じ)
- ageMonths=0, custom=200, type='feeding' -> 200 (カスタム優先)
- ageMonths=0, custom=null, type='diaper' -> 180
- ageMonths=1, custom=null, type='diaper' -> 240
- ageMonths=6, custom=null, type='diaper' -> 360
- ageMonths=2, custom=300, type='diaper' -> 300 (カスタム優先)

**indicatorUtils — calcProgress:**
- lastRecordTimeISO=null -> 0
- 経過60分, 閾値120分 -> 0.5
- 経過120分, 閾値120分 -> 1.0
- 経過180分, 閾値120分 -> 1.5 (閾値超過でも生値を返す)

**indicatorUtils — getIndicatorColor:**
- progress=0.0 -> 'green', progress=0.59 -> 'green'
- progress=0.6 -> 'yellow', progress=0.79 -> 'yellow'
- progress=0.8 -> 'red', progress=1.5 -> 'red'

**indicatorUtils — isOverThreshold:**
- progress=0.99 -> false, progress=1.0 -> true, progress=1.5 -> true
  </behavior>
  <implementation>
06-02-PLAN.md で frontend/lib/thresholdUtils.ts と frontend/lib/indicatorUtils.ts を作成してテストを GREEN にする。
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: ワークツリー作成 + thresholdUtils テスト作成（TDD RED）</name>
  <files>frontend/__tests__/thresholdUtils.test.ts</files>
  <action>
まずワークツリーを作成する（CLAUDE.md 必須）:
```bash
sh scripts/setup_worktree.sh feat/phase-6-frontend-indicators
```

ワークツリーのパス: /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators
以降の全操作はこのパスで行う。

frontend/__tests__/thresholdUtils.test.ts を作成する:

```typescript
import { describe, it, expect } from 'vitest'
import { resolveThreshold } from '@/lib/thresholdUtils'

describe('resolveThreshold', () => {
  describe('feeding — WHOガイドライン閾値（カスタムなし）', () => {
    it('生後0ヶ月は 120分 を返す', () => {
      expect(resolveThreshold(0, null, 'feeding')).toBe(120)
    })

    it('生後1ヶ月は 150分 を返す', () => {
      expect(resolveThreshold(1, null, 'feeding')).toBe(150)
    })

    it('生後2ヶ月は 150分 を返す', () => {
      expect(resolveThreshold(2, null, 'feeding')).toBe(150)
    })

    it('生後3ヶ月は 180分 を返す', () => {
      expect(resolveThreshold(3, null, 'feeding')).toBe(180)
    })

    it('生後5ヶ月は 180分 を返す', () => {
      expect(resolveThreshold(5, null, 'feeding')).toBe(180)
    })

    it('生後6ヶ月は 240分 を返す', () => {
      expect(resolveThreshold(6, null, 'feeding')).toBe(240)
    })

    it('生後12ヶ月は 240分 を返す（6ヶ月以上は全て同じ）', () => {
      expect(resolveThreshold(12, null, 'feeding')).toBe(240)
    })
  })

  describe('diaper — WHOガイドライン閾値（カスタムなし）', () => {
    it('生後0ヶ月は 180分 を返す', () => {
      expect(resolveThreshold(0, null, 'diaper')).toBe(180)
    })

    it('生後1ヶ月は 240分 を返す', () => {
      expect(resolveThreshold(1, null, 'diaper')).toBe(240)
    })

    it('生後5ヶ月は 240分 を返す', () => {
      expect(resolveThreshold(5, null, 'diaper')).toBe(240)
    })

    it('生後6ヶ月は 360分 を返す', () => {
      expect(resolveThreshold(6, null, 'diaper')).toBe(360)
    })

    it('生後12ヶ月は 360分 を返す（6ヶ月以上は全て同じ）', () => {
      expect(resolveThreshold(12, null, 'diaper')).toBe(360)
    })
  })

  describe('カスタム閾値優先', () => {
    it('feeding: customMinutes が数値なら customMinutes を返す', () => {
      expect(resolveThreshold(0, 200, 'feeding')).toBe(200)
    })

    it('feeding: 月齢6ヶ月でも customMinutes=90 なら 90 を返す', () => {
      expect(resolveThreshold(6, 90, 'feeding')).toBe(90)
    })

    it('diaper: customMinutes が数値なら customMinutes を返す', () => {
      expect(resolveThreshold(2, 300, 'diaper')).toBe(300)
    })

    it('customMinutes=undefined のときはガイドライン値を返す', () => {
      expect(resolveThreshold(0, undefined, 'feeding')).toBe(120)
    })
  })
})
```

テスト実行して全件 FAIL を確認する（lib/thresholdUtils.ts 未作成のため）:
```bash
pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators && pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts 2>&1 | grep -E "FAIL|Cannot find|Error" | head -5</automated>
  </verify>
  <done>
- frontend/__tests__/thresholdUtils.test.ts が存在する
- pnpm test 実行で全テストが FAIL（モジュール未解決）
- テスト数: 16件以上
  </done>
</task>

<task type="auto">
  <name>Task 2: indicatorUtils テスト作成（TDD RED）+ コミット</name>
  <files>frontend/__tests__/indicatorUtils.test.ts</files>
  <action>
frontend/__tests__/indicatorUtils.test.ts を作成する:

```typescript
import { describe, it, expect, afterEach, vi } from 'vitest'
import { calcProgress, getIndicatorColor, isOverThreshold, INDICATOR_COLORS } from '@/lib/indicatorUtils'

describe('indicatorUtils', () => {
  afterEach(() => {
    vi.useRealTimers()
  })

  describe('calcProgress', () => {
    it('lastRecordTimeISO が null のとき 0 を返す', () => {
      expect(calcProgress(null, 120)).toBe(0)
    })

    it('経過60分, 閾値120分 → 0.5', () => {
      vi.setSystemTime(new Date('2024-05-20T11:00:00Z'))
      expect(calcProgress('2024-05-20T10:00:00Z', 120)).toBeCloseTo(0.5)
    })

    it('経過120分, 閾値120分 → 1.0（ちょうど閾値）', () => {
      vi.setSystemTime(new Date('2024-05-20T12:00:00Z'))
      expect(calcProgress('2024-05-20T10:00:00Z', 120)).toBeCloseTo(1.0)
    })

    it('経過180分, 閾値120分 → 1.5（閾値超過でも生値を返す）', () => {
      vi.setSystemTime(new Date('2024-05-20T13:00:00Z'))
      expect(calcProgress('2024-05-20T10:00:00Z', 120)).toBeCloseTo(1.5)
    })

    it('nowISO を指定した場合はそちらを現在時刻として使用する', () => {
      const result = calcProgress('2024-05-20T10:00:00Z', 120, '2024-05-20T11:00:00Z')
      expect(result).toBeCloseTo(0.5)
    })

    it('経過0分のとき 0 を返す', () => {
      vi.setSystemTime(new Date('2024-05-20T10:00:00Z'))
      expect(calcProgress('2024-05-20T10:00:00Z', 120)).toBeCloseTo(0)
    })
  })

  describe('getIndicatorColor', () => {
    it('progress=0 → green', () => {
      expect(getIndicatorColor(0)).toBe('green')
    })

    it('progress=0.59 → green', () => {
      expect(getIndicatorColor(0.59)).toBe('green')
    })

    it('progress=0.6 → yellow', () => {
      expect(getIndicatorColor(0.6)).toBe('yellow')
    })

    it('progress=0.79 → yellow', () => {
      expect(getIndicatorColor(0.79)).toBe('yellow')
    })

    it('progress=0.8 → red', () => {
      expect(getIndicatorColor(0.8)).toBe('red')
    })

    it('progress=1.0 → red', () => {
      expect(getIndicatorColor(1.0)).toBe('red')
    })

    it('progress=1.5 → red（閾値超過）', () => {
      expect(getIndicatorColor(1.5)).toBe('red')
    })
  })

  describe('isOverThreshold', () => {
    it('progress=0.99 → false', () => {
      expect(isOverThreshold(0.99)).toBe(false)
    })

    it('progress=1.0 → true', () => {
      expect(isOverThreshold(1.0)).toBe(true)
    })

    it('progress=1.5 → true', () => {
      expect(isOverThreshold(1.5)).toBe(true)
    })
  })

  describe('INDICATOR_COLORS', () => {
    it('green, yellow, red の3色定数が文字列として存在する', () => {
      expect(typeof INDICATOR_COLORS.green).toBe('string')
      expect(typeof INDICATOR_COLORS.yellow).toBe('string')
      expect(typeof INDICATOR_COLORS.red).toBe('string')
    })
  })
})
```

テスト実行して全件 FAIL を確認した後、両テストファイルをコミットする:
```bash
git add frontend/__tests__/thresholdUtils.test.ts frontend/__tests__/indicatorUtils.test.ts
git commit -m "test(06-01): thresholdUtils と indicatorUtils の TDD RED テスト追加"
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators && pnpm --filter frontend test -- --run __tests__/indicatorUtils.test.ts 2>&1 | grep -E "FAIL|Cannot find|Error" | head -5</automated>
  </verify>
  <done>
- frontend/__tests__/indicatorUtils.test.ts が存在する
- pnpm test 実行で全テストが FAIL
- テスト数: 17件以上
- 両ファイルが git commit 済み
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators

# 両テストファイルが存在すること
ls frontend/__tests__/thresholdUtils.test.ts frontend/__tests__/indicatorUtils.test.ts

# 全テストが FAIL であること（RED 確認）
pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts __tests__/indicatorUtils.test.ts 2>&1 | grep -E "FAIL|passed|failed"

# git コミット済み
git log --oneline -3
```
</verification>

<success_criteria>
1. frontend/__tests__/thresholdUtils.test.ts が存在し、16件以上のテストが記述されている
2. frontend/__tests__/indicatorUtils.test.ts が存在し、17件以上のテストが記述されている
3. 両テストを実行すると全件 FAIL（lib/thresholdUtils.ts と lib/indicatorUtils.ts が未作成のため）
4. テストファイルが git コミット済み
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-indicators/06-01-SUMMARY.md`
</output>
