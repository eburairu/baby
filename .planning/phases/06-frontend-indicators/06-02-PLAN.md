---
phase: 06-frontend-indicators
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - frontend/lib/thresholdUtils.ts
  - frontend/lib/indicatorUtils.ts
autonomous: true
requirements:
  - THRES-01
  - INDIC-03
  - INDIC-04

must_haves:
  truths:
    - "resolveThreshold が月齢に基づくWHOガイドライン閾値を返す（カスタムなし時）"
    - "resolveThreshold がカスタム閾値を優先して返す（カスタムあり時）"
    - "calcProgress が経過時間/閾値の比率（0以上）を返す"
    - "getIndicatorColor が進捗率に基づき green/yellow/red を返す"
    - "isOverThreshold が progress >= 1.0 のとき true を返す"
    - "vitest の thresholdUtils・indicatorUtils テストが全件 GREEN"
  artifacts:
    - path: "frontend/lib/thresholdUtils.ts"
      provides: "resolveThreshold 関数"
      exports: ["resolveThreshold"]
    - path: "frontend/lib/indicatorUtils.ts"
      provides: "calcProgress, getIndicatorColor, isOverThreshold, INDICATOR_COLORS"
      exports: ["calcProgress", "getIndicatorColor", "isOverThreshold", "INDICATOR_COLORS"]
  key_links:
    - from: "frontend/lib/thresholdUtils.ts"
      to: "frontend/app/(dashboard)/dashboard/page.tsx"
      via: "resolveThreshold(calcAge(baby.birthday).months, baby.feeding_threshold_minutes, 'feeding')"
      pattern: "resolveThreshold"
    - from: "frontend/lib/indicatorUtils.ts"
      to: "frontend/components/dashboard/FeedingWidget.tsx"
      via: "calcProgress(lastFeedingTime, thresholdMinutes)"
      pattern: "calcProgress"
---

<objective>
Phase 6 の TDD GREEN フェーズ（Wave 2）。06-01 で書いたテストを GREEN にするため、thresholdUtils と indicatorUtils の実装を作成する。

Purpose: THRES-01（月齢ベース閾値自動決定）と INDIC-03/04 のロジックをピュア関数として実装し、テスタブルな形でカプセル化する。
Output: frontend/lib/thresholdUtils.ts と frontend/lib/indicatorUtils.ts（全テスト PASS）。
</objective>

<execution_context>
@/Users/ry1e/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ry1e/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-indicators/06-01-SUMMARY.md

<interfaces>
<!-- 実装対象関数のシグネチャと期待する挙動 -->

thresholdUtils.ts で実装する関数:
```typescript
// WHOガイドライン閾値テーブル
// feeding: { 0: 120, 1: 150, 3: 180, 6: 240 } (月齢の下限でグループ分け)
// diaper:  { 0: 180, 1: 240, 6: 360 }

export function resolveThreshold(
  ageMonths: number,
  customMinutes: number | null | undefined,
  type: 'feeding' | 'diaper'
): number {
  // customMinutes が null/undefined でない場合はカスタム値を返す
  // それ以外は月齢に基づくガイドライン値を返す（月齢テーブルで境界を下から検索）
}
```

indicatorUtils.ts で実装する関数:
```typescript
export const INDICATOR_COLORS = {
  green: '#22c55e',
  yellow: '#eab308',
  red: '#ef4444',
}

export function calcProgress(
  lastRecordTimeISO: string | null,
  thresholdMinutes: number,
  nowISO?: string
): number {
  // lastRecordTimeISO が null → return 0
  // nowISO が指定されていれば new Date(nowISO)、なければ new Date()
  // elapsedMinutes = (now - lastRecord) / 60000
  // return elapsedMinutes / thresholdMinutes  (クランプしない)
}

export function getIndicatorColor(progress: number): 'green' | 'yellow' | 'red' {
  // progress < 0.6 → 'green'
  // progress < 0.8 → 'yellow'
  // else → 'red'
}

export function isOverThreshold(progress: number): boolean {
  return progress >= 1.0
}
```

テストファイル（06-01 で作成済み）:
- frontend/__tests__/thresholdUtils.test.ts
- frontend/__tests__/indicatorUtils.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: thresholdUtils.ts の実装（TDD GREEN）</name>
  <files>frontend/lib/thresholdUtils.ts</files>
  <action>
frontend/lib/thresholdUtils.ts を新規作成する:

```typescript
/**
 * WHO/小児科ガイドラインに基づく授乳・おむつ警告閾値を解決する
 *
 * 月齢ブレイクポイント（下限値で管理）:
 *   feeding: 0ヶ月=120分, 1ヶ月=150分, 3ヶ月=180分, 6ヶ月=240分
 *   diaper:  0ヶ月=180分, 1ヶ月=240分, 6ヶ月=360分
 *
 * カスタム閾値（null/undefined でない数値）が指定された場合はカスタム値を優先する。
 */

const FEEDING_THRESHOLDS: [number, number][] = [
  [6, 240],
  [3, 180],
  [1, 150],
  [0, 120],
]

const DIAPER_THRESHOLDS: [number, number][] = [
  [6, 360],
  [1, 240],
  [0, 180],
]

export function resolveThreshold(
  ageMonths: number,
  customMinutes: number | null | undefined,
  type: 'feeding' | 'diaper'
): number {
  if (customMinutes != null) {
    return customMinutes
  }

  const table = type === 'feeding' ? FEEDING_THRESHOLDS : DIAPER_THRESHOLDS
  for (const [minAge, minutes] of table) {
    if (ageMonths >= minAge) {
      return minutes
    }
  }
  // fallback（ageMonths が負の場合など）
  return table[table.length - 1][1]
}
```

実装後にテストを実行して全件 GREEN を確認する:
```bash
pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts 2>&1 | tail -20
```

全件 PASS 後にコミット:
```bash
git add frontend/lib/thresholdUtils.ts
git commit -m "feat(06-02): thresholdUtils — WHOガイドライン閾値解決関数を実装"
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators && pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts 2>&1 | grep -E "passed|failed|PASS|FAIL" | head -5</automated>
  </verify>
  <done>
- frontend/lib/thresholdUtils.ts が存在する
- pnpm test で thresholdUtils.test.ts の全テストが PASS（GREEN）
- git commit 済み
  </done>
</task>

<task type="auto">
  <name>Task 2: indicatorUtils.ts の実装（TDD GREEN）+ コミット</name>
  <files>frontend/lib/indicatorUtils.ts</files>
  <action>
frontend/lib/indicatorUtils.ts を新規作成する:

```typescript
/**
 * 経過時間インジケーターのユーティリティ関数
 *
 * progress = 経過時間 / 閾値（0以上、1.0超も許容）
 * 色閾値: green < 0.6 <= yellow < 0.8 <= red
 */

export const INDICATOR_COLORS = {
  green: '#22c55e',
  yellow: '#eab308',
  red: '#ef4444',
} as const

/**
 * 前回記録からの経過割合を返す
 * @param lastRecordTimeISO 前回記録のISO文字列（nullの場合は0を返す）
 * @param thresholdMinutes 警告閾値（分）
 * @param nowISO 現在時刻のISO文字列（省略時は Date.now() を使用）
 * @returns 0以上の数値（1.0超 = 閾値超過）
 */
export function calcProgress(
  lastRecordTimeISO: string | null,
  thresholdMinutes: number,
  nowISO?: string
): number {
  if (lastRecordTimeISO === null) {
    return 0
  }
  const now = nowISO ? new Date(nowISO).getTime() : Date.now()
  const last = new Date(lastRecordTimeISO).getTime()
  const elapsedMinutes = (now - last) / 60000
  return elapsedMinutes / thresholdMinutes
}

/**
 * 進捗率から表示色を決定する
 */
export function getIndicatorColor(progress: number): 'green' | 'yellow' | 'red' {
  if (progress < 0.6) return 'green'
  if (progress < 0.8) return 'yellow'
  return 'red'
}

/**
 * 閾値を超過しているかどうかを返す（点滅表示の判定に使用）
 */
export function isOverThreshold(progress: number): boolean {
  return progress >= 1.0
}
```

実装後にテストを実行して全件 GREEN を確認する:
```bash
pnpm --filter frontend test -- --run __tests__/indicatorUtils.test.ts 2>&1 | tail -20
```

両ファイルの全テスト GREEN を確認してコミット:
```bash
pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts __tests__/indicatorUtils.test.ts 2>&1 | grep -E "passed|failed"
git add frontend/lib/indicatorUtils.ts
git commit -m "feat(06-02): indicatorUtils — 経過時間プログレス・色・閾値超過判定を実装"
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators && pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts __tests__/indicatorUtils.test.ts 2>&1 | grep -E "passed|failed|PASS|FAIL" | head -5</automated>
  </verify>
  <done>
- frontend/lib/indicatorUtils.ts が存在する
- thresholdUtils.test.ts と indicatorUtils.test.ts の全テストが PASS（GREEN）
- git commit 済み
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators

# 両実装ファイルが存在すること
ls frontend/lib/thresholdUtils.ts frontend/lib/indicatorUtils.ts

# 全テストが GREEN であること
pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts __tests__/indicatorUtils.test.ts 2>&1 | grep -E "passed|failed"

# git コミット済み
git log --oneline -5
```
</verification>

<success_criteria>
1. frontend/lib/thresholdUtils.ts が存在し、resolveThreshold がエクスポートされている
2. frontend/lib/indicatorUtils.ts が存在し、calcProgress・getIndicatorColor・isOverThreshold・INDICATOR_COLORS がエクスポートされている
3. pnpm test で thresholdUtils.test.ts と indicatorUtils.test.ts の全テストが PASS（GREEN）
4. 両ファイルが git commit 済み
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-indicators/06-02-SUMMARY.md`
</output>
