---
phase: 06-frontend-indicators
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - frontend/types/widget.ts
  - frontend/components/dashboard/FeedingWidget.tsx
  - frontend/components/dashboard/DiaperWidget.tsx
  - frontend/app/(dashboard)/dashboard/page.tsx
autonomous: false
requirements:
  - INDIC-01
  - INDIC-02
  - INDIC-03
  - INDIC-04
  - INDIC-05
  - THRES-01

must_haves:
  truths:
    - "授乳ウィジェットの六角形外周にプログレスストロークが表示される"
    - "おむつウィジェットの六角形外周にプログレスストロークが表示される"
    - "ストロークが経過率に応じて緑/黄/赤に変化する"
    - "閾値超過時にストロークが点滅する"
    - "画面を開いたまま1分放置するとストロークが更新される（setInterval 60秒）"
    - "月齢ベース閾値が自動決定され、dashboard/page.tsx から Widget に渡される"
  artifacts:
    - path: "frontend/types/widget.ts"
      provides: "BaseWidgetProps に thresholdMinutes を追加"
    - path: "frontend/components/dashboard/FeedingWidget.tsx"
      provides: "1分更新・インジケーター表示対応の授乳ウィジェット"
    - path: "frontend/components/dashboard/DiaperWidget.tsx"
      provides: "1分更新・インジケーター表示対応のおむつウィジェット"
    - path: "frontend/app/(dashboard)/dashboard/page.tsx"
      provides: "resolveThreshold で計算した閾値を各ウィジェットに渡す"
  key_links:
    - from: "frontend/app/(dashboard)/dashboard/page.tsx"
      to: "frontend/components/dashboard/FeedingWidget.tsx"
      via: "thresholdMinutes={feedingThreshold}"
      pattern: "thresholdMinutes.*feedingThreshold"
    - from: "frontend/components/dashboard/FeedingWidget.tsx"
      to: "frontend/components/dashboard/HexagonWidgetCard.tsx"
      via: "indicatorProgress={progress} isOverThreshold={isOver}"
      pattern: "indicatorProgress"
    - from: "frontend/components/dashboard/DiaperWidget.tsx"
      to: "frontend/components/dashboard/HexagonWidgetCard.tsx"
      via: "indicatorProgress={progress} isOverThreshold={isOver}"
      pattern: "indicatorProgress"
---

<objective>
Phase 6 の統合フェーズ（Wave 3）。06-02 と 06-03 の成果物を繋ぎ込み、FeedingWidget と DiaperWidget に1分更新インジケーターを組み込む。dashboard/page.tsx から thresholdMinutes を渡すことで THRES-01 を実現する。

Purpose: 全 INDIC 要件（01-05）と THRES-01 を一気通貫で実現する最終統合。
Output: 動作するインジケーター付きウィジェット（ダッシュボード上で目視確認可能）。
</objective>

<execution_context>
@/Users/ry1e/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ry1e/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-indicators/06-02-SUMMARY.md
@.planning/phases/06-frontend-indicators/06-03-SUMMARY.md

<interfaces>
<!-- 変更対象ファイルの現在のインターフェース。エグゼキューターはここから出発する。 -->

frontend/types/widget.ts（現在）:
```typescript
export interface BaseWidgetProps {
  babyId: number | string
  records?: unknown[]
  isError?: boolean
  mutate?: () => void
  isLoading?: boolean
  size?: number
}
```
追加: `thresholdMinutes?: number`

frontend/components/dashboard/FeedingWidget.tsx（現在の構造）:
```typescript
// Props: BaseWidgetProps
// Uses: calculateFeedingStats(feedingRecords) -> { lastFeedingTime, lastElapsed, todayCount, ... }
// lastFeedingTime: string | null (ISO形式)
// HexagonWidgetCard に content を渡す
```

frontend/components/dashboard/DiaperWidget.tsx（現在の構造）:
```typescript
// Props: BaseWidgetProps
// Uses: calculateDiaperStats(diaperRecords) -> { lastElapsed, wetCount, dirtyCount }
// diapers[0] が最新おむつ記録（timestamp フィールドを持つ）
```

frontend/app/(dashboard)/dashboard/page.tsx（現在の構造）:
```typescript
// selectedBaby: { id, birthday?: string, feeding_threshold_minutes?: number | null,
//                 diaper_threshold_minutes?: number | null, ... }
// 現在の使用箇所:
//   <FeedingWidget babyId={selectedBaby.id} ... />
//   <DiaperWidget babyId={selectedBaby.id} ... />
```

利用するユーティリティ（06-02 で作成済み）:
```typescript
import { resolveThreshold } from '@/lib/thresholdUtils'
// resolveThreshold(ageMonths, customMinutes, type) -> number

import { calcProgress, isOverThreshold } from '@/lib/indicatorUtils'
// calcProgress(lastRecordTimeISO, thresholdMinutes) -> number
// isOverThreshold(progress) -> boolean
```

frontend/lib/ageUtils.ts（既存）:
```typescript
import { calcAge } from '@/lib/ageUtils'
// calcAge(birthday: string): { months: number, days: number, label: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseWidgetProps 拡張 + FeedingWidget・DiaperWidget にインジケーター組み込み</name>
  <files>
    frontend/types/widget.ts
    frontend/components/dashboard/FeedingWidget.tsx
    frontend/components/dashboard/DiaperWidget.tsx
  </files>
  <action>
**Step 1: frontend/types/widget.ts を読んで thresholdMinutes を追加する**

BaseWidgetProps に `thresholdMinutes?: number` を追加する。

**Step 2: FeedingWidget.tsx を読んでインジケーターを追加する**

追加内容:
1. インポート: `import { useState, useEffect } from 'react'` と `import { calcProgress, isOverThreshold } from '@/lib/indicatorUtils'`
2. 1分更新 tick state（INDIC-05）:
```typescript
const [tick, setTick] = useState(0)
useEffect(() => {
  const id = setInterval(() => setTick(t => t + 1), 60000)
  return () => clearInterval(id)
}, [])
```
3. calculateFeedingStats の直後に追加:
```typescript
const progress = thresholdMinutes != null
  ? calcProgress(lastFeedingTime, thresholdMinutes)
  : 0
const isOver = isOverThreshold(progress)
```
4. HexagonWidgetCard に props 追加:
```tsx
indicatorProgress={thresholdMinutes != null ? progress : undefined}
isOverThreshold={isOver}
```

Note: tick は setInterval で setState を呼ぶことで再レンダリングを促す。
progress 計算の引数に tick を入れる必要はない（再レンダリングで calcProgress が再実行される）。

**Step 3: DiaperWidget.tsx を読んでインジケーターを追加する**

FeedingWidget と同じパターンを適用する。
- 最新おむつ記録の時刻: `diapers[0]?.timestamp` を使う（calculateDiaperStats の結果からでも可）
- 実際のコードを読んで lastDiaperTime（または相当する変数）を確認してから calcProgress に渡す

変更後にビルドチェック:
```bash
pnpm --filter frontend build 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators && grep "thresholdMinutes" frontend/types/widget.ts && grep "indicatorProgress" frontend/components/dashboard/FeedingWidget.tsx && grep "indicatorProgress" frontend/components/dashboard/DiaperWidget.tsx</automated>
  </verify>
  <done>
- frontend/types/widget.ts に thresholdMinutes?: number が追加されている
- FeedingWidget.tsx に tick state・calcProgress・indicatorProgress props が追加されている
- DiaperWidget.tsx に同様のパターンが追加されている
- pnpm build が通る（TypeScript エラーなし）
  </done>
</task>

<task type="auto">
  <name>Task 2: dashboard/page.tsx で閾値を計算してウィジェットに渡す</name>
  <files>frontend/app/(dashboard)/dashboard/page.tsx</files>
  <action>
dashboard/page.tsx を読んで以下の変更を加える。

**インポート追加:**
```typescript
import { resolveThreshold } from '@/lib/thresholdUtils'
import { calcAge } from '@/lib/ageUtils'
```

**selectedBaby が利用可能な箇所（FeedingWidget・DiaperWidget のレンダリング直前）に追加:**
```typescript
const ageMonths = selectedBaby?.birthday
  ? calcAge(selectedBaby.birthday).months
  : 0

const feedingThreshold = resolveThreshold(
  ageMonths,
  selectedBaby?.feeding_threshold_minutes,
  'feeding'
)
const diaperThreshold = resolveThreshold(
  ageMonths,
  selectedBaby?.diaper_threshold_minutes,
  'diaper'
)
```

**FeedingWidget と DiaperWidget に thresholdMinutes を渡す:**
```tsx
<FeedingWidget
  ...既存props
  thresholdMinutes={feedingThreshold}
/>
<DiaperWidget
  ...既存props
  thresholdMinutes={diaperThreshold}
/>
```

変更後にビルドチェックしてコミット:
```bash
pnpm --filter frontend build 2>&1 | tail -20
git add frontend/types/widget.ts frontend/components/dashboard/FeedingWidget.tsx frontend/components/dashboard/DiaperWidget.tsx frontend/app/(dashboard)/dashboard/page.tsx
git commit -m "feat(06-04): インジケーター統合 — FeedingWidget・DiaperWidget・ダッシュボードに閾値連携を追加"
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators && grep "thresholdMinutes={feedingThreshold}" frontend/app/(dashboard)/dashboard/page.tsx && echo "wired OK"</automated>
  </verify>
  <done>
- dashboard/page.tsx が resolveThreshold を使って feedingThreshold・diaperThreshold を計算している
- FeedingWidget と DiaperWidget に thresholdMinutes が渡されている
- pnpm build が通る
- git commit 済み
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ダッシュボードでインジケーター動作を目視確認</name>
  <files></files>
  <action>
自動化された実装の目視確認。Claude は sh scripts/verify_all.sh を実行してビルドと全テストが通ることを確認してから人間に渡す。
```bash
sh scripts/verify_all.sh 2>&1 | tail -30
```
  </action>
  <verify>
    <automated>cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators && sh scripts/verify_all.sh 2>&1 | tail -10</automated>
  </verify>
  <done>
- ダッシュボードで授乳・おむつウィジェットにストロークが表示されている
- 色が経過率に応じて変化することが確認できた
- 人間が「approved」と入力した
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/ry1e/Documents/work/baby/worktrees/feat/phase-6-frontend-indicators

# 全ユニットテストが GREEN
pnpm --filter frontend test -- --run __tests__/thresholdUtils.test.ts __tests__/indicatorUtils.test.ts 2>&1 | grep -E "passed|failed"

# TypeScript ビルドが通る
pnpm --filter frontend build 2>&1 | tail -10

# 統合確認
grep "thresholdMinutes" frontend/components/dashboard/FeedingWidget.tsx
grep "thresholdMinutes" frontend/components/dashboard/DiaperWidget.tsx
grep "feedingThreshold" frontend/app/(dashboard)/dashboard/page.tsx

# 全体チェック
sh scripts/verify_all.sh 2>&1 | tail -20
```
</verification>

<success_criteria>
1. 授乳ウィジェットとおむつウィジェットの六角形外周に経過時間ストロークが表示される（INDIC-01, INDIC-02）
2. ストロークの色が緑/黄/赤と経過率に応じて変化する（INDIC-03）
3. 閾値超過時にストロークが点滅する（INDIC-04）
4. 1分ごとにストロークが自動更新される（INDIC-05）
5. 月齢ベース閾値が resolveThreshold で自動決定される（THRES-01）
6. pnpm build・全ユニットテスト・verify_all.sh が全て通る
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-indicators/06-04-SUMMARY.md`
</output>
