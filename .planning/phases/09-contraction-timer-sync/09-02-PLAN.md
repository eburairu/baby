---
phase: 09-contraction-timer-sync
plan: 02
type: execute
wave: 2
depends_on: ["09-01-PLAN.md"]
files_modified: ["frontend/hooks/useContractionTimerSync.ts"]
autonomous: true
requirements: ["TIMER-FE-01", "TIMER-FE-02"]

must_haves:
  truths:
    - "useContractionTimerSync フックが存在する"
    - "3秒（3000ms）間隔でポーリングが行われる"
    - "取得したデータが contractionStore に同期される"
  artifacts:
    - path: "frontend/hooks/useContractionTimerSync.ts"
      provides: "バックエンドのタイマー状態をポーリング同期するフック"
  key_links:
    - "SWR -> contractionStore.sync() (取得データの反映)"
    - "useContractionTimerSync -> ContractionTimer (mutate の提供)"
---

<objective>
frontend/hooks/useContractionTimerSync.ts を作成し、useSWR を使用してバックエンドの陣痛タイマー状態をポーリング同期する。

目的: 3秒ごとにバックエンドから最新のタイマー状態（status, start_time）を取得し、ストアに反映させることでデバイス間同期を実現する。
出力: `useContractionTimerSync` カスタムフック。
</objective>

<execution_context>
@/Users/ry1e/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/stores/contractionStore.ts
@frontend/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useContractionTimerSync フックの実装</name>
  <files>frontend/hooks/useContractionTimerSync.ts</files>
  <action>
    - `useContractionTimerSync(babyId: number | null)` を定義。
    - `useSWR` を使用して `/api/babies/${babyId}/timer/contraction` をフェッチ。
    - オプション: `refreshInterval: 3000`, `revalidateOnFocus: true` を設定。
    - `mutate` をフックから返却するように実装。
    - `useEffect` を使用して、データが更新されたら `contractionStore` の `sync` メソッドを呼び出す。
    - データが `null` または `undefined` の場合は `sync('idle', null)` を呼び出す（初期状態）。
  </action>
  <verify>
    - フックがエラーなくロードされ、ネットワークタブで 3秒ごとにリクエストが飛んでいることを確認。
  </verify>
  <done>
    `useContractionTimerSync` フックが実装され、ポーリングが機能している。
  </done>
</task>

</tasks>

<verification>
コンポーネントで `useContractionTimerSync(babyId)` を呼び出し、サーバーの DB 状態を変更した際に 3秒以内にストアの状態が更新されることを確認。
</verification>

<success_criteria>
- `useContractionTimerSync` フックが実装されている。
- SWR によるポーリングが 3秒間隔で実行される。
- `mutate` が正しくエクスポートされている。
- 取得した `status` と `start_time` が `contractionStore` に正しく同期される。
</success_criteria>

<output>
After completion, create `.planning/phases/09-contraction-timer-sync/09-02-SUMMARY.md`
</output>
