---
phase: 09-contraction-timer-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["frontend/stores/contractionStore.ts"]
autonomous: true
requirements: ["TIMER-FE-01", "TIMER-FE-02"]

must_haves:
  truths:
    - "contractionStore に sync メソッドが存在する"
    - "sync(status, startTime) を呼び出すと、状態が更新される"
    - "sync 時に経過時間 (elapsedSeconds) が再計算される"
  artifacts:
    - path: "frontend/stores/contractionStore.ts"
      provides: "外部から状態を同期するインターフェース"
  key_links:
    - "contractionStore.sync() -> useContractionTimerSync (SWR からの更新)"
---

<objective>
frontend/stores/contractionStore.ts に `sync` メソッドを追加し、外部（SWRポーリングなど）から状態（status, startTime）を上書き可能にする。

目的: バックエンドのタイマー状態とフロントエンドの Zustand ストアを同期させるための口を作る。
出力: `sync` メソッドが追加された `useContractionTimer` ストア。
</objective>

<execution_context>
@/Users/ry1e/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/stores/contractionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: sync メソッドの型定義と実装</name>
  <files>frontend/stores/contractionStore.ts</files>
  <action>
    - `ContractionTimerState` インターフェースに `sync: (status: TimerStatus, startTime: string | Date | null) => void` を追加。
    - `sync` メソッドの実装:
      1. 引数の `startTime` が文字列の場合は `Date` オブジェクトに変換する。
      2. `status` と `startTime` をセットする。
      3. `status` が 'timing' かつ `startTime` が存在する場合、現在時刻との差分から `elapsedSeconds` を計算してセットする。
      4. それ以外の場合は `elapsedSeconds` を 0 にセットする。
  </action>
  <verify>
    npm test (既存のテストがあれば実行、なければ手動でストアを呼び出して確認)
  </verify>
  <done>
    `sync` メソッドがストアに追加され、正しく状態を反映できる。
  </done>
</task>

</tasks>

<verification>
`useContractionTimer.getState().sync('timing', new Date(Date.now() - 10000))` を実行した際、`status` が 'timing'、`elapsedSeconds` が約 10 になることを確認。
</verification>

<success_criteria>
- ストアに `sync` メソッドが実装されている。
- `sync` を呼ぶことで `status`, `startTime`, `elapsedSeconds` が一貫性のある状態で更新される。
</success_criteria>

<output>
After completion, create `.planning/phases/09-contraction-timer-sync/09-01-SUMMARY.md`
</output>
