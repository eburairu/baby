---
phase: 09-contraction-timer-sync
plan: 03
type: execute
wave: 3
depends_on: ["09-02-PLAN.md"]
files_modified: ["frontend/components/contraction/ContractionTimer.tsx"]
autonomous: true
requirements: ["TIMER-FE-01", "TIMER-FE-02"]

must_haves:
  truths:
    - "ContractionTimer で useContractionTimerSync が呼び出されている"
    - "開始/停止時に PUT リクエストが送信される"
    - "mutate() によるキャッシュ更新が行われる"
  artifacts:
    - path: "frontend/components/contraction/ContractionTimer.tsx"
      provides: "操作内容をバックエンドに即座に反映し、同期フックを有効化した UI"
  key_links:
    - "ContractionTimer -> useContractionTimerSync (ポーリング有効化)"
    - "ContractionTimer -> PUT /api/babies/{babyId}/timer/contraction (状態更新)"
---

<objective>
frontend/components/contraction/ContractionTimer.tsx を修正し、タイマー操作をバックエンドと同期させる。

目的: 「開始」「停止」ボタンクリック時にバックエンドの状態を即座に更新し、かつ同期フック（useContractionTimerSync）を組み込むことで、自身の操作と他デバイスからの同期の両方を実現する。
出力: 同期機能が統合された `ContractionTimer` コンポーネント。
</objective>

<execution_context>
@/Users/ry1e/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/components/contraction/ContractionTimer.tsx
@frontend/hooks/useContractionTimerSync.ts
@frontend/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContractionTimer の修正</name>
  <files>frontend/components/contraction/ContractionTimer.tsx</files>
  <action>
    - `useContractionTimerSync(babyId)` フックを呼び出し、`mutate` を取得。
    - `handleToggle` メソッドの修正:
      1. `status === "idle"` (開始時):
         - `const startTime = new Date(Date.now() - offsetMs)` をローカルで作成。
         - `start(offsetMs)` でローカル状態を更新。
         - `await api.put(`/api/babies/${babyId}/timer/contraction`, { status: "timing", start_time: startTime.toISOString() })` を送信。
         - 送信後、`mutate()` を呼び出してキャッシュを更新。
      2. `status === "timing"` (終了時):
         - `const result = stop()` でローカル状態を停止（タイマー記録用のデータ取得）。
         - `await api.put(`/api/babies/${babyId}/timer/contraction`, { status: "idle", start_time: null })` を送信。
         - `if (result)` の場合、並行して `api.post("/contractions/", ...)` で記録を保存。
         - 両方の完了（または順序実行）を待ち、`mutate()` を呼び出してキャッシュを更新。
         - 記録保存に失敗した場合はトーストで通知する（既存ロジック維持）。
  </action>
  <verify>
    - デバイスAでボタンを押して開始し、デバイスBで 3秒以内に状態が反映されるか。
    - 逆に、デバイスBで停止した際にデバイスAの表示が停止に変わるか。
  </verify>
  <done>
    `ContractionTimer` がバックエンドと完全同期する。
  </done>
</task>

</tasks>

<verification>
実機またはブラウザ2画面で、一方が開始した際に他方が 3秒以内に「計測中」になり、一方が停止した際に他方が「ボタンを押して計測開始」になることを目視で確認。
</verification>

<success_criteria>
- ボタンクリック時に `PUT /api/babies/{babyId}/timer/contraction` が正しく送信される。
- `useContractionTimerSync` が有効であり、他デバイスからの操作が反映される。
- 計測終了時の `POST /contractions/` による記録作成が引き続き正常に機能する。
</success_criteria>

<output>
After completion, create `.planning/phases/09-contraction-timer-sync/09-03-SUMMARY.md`
</output>
