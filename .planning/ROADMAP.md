# ロードマップ: ダッシュボードUIのハニカム構造化

## フェーズ 1: ハニカムウィジェットの設計
ハニカム構造に適したウィジェット用のコンポーネントを作成する。
- `HexagonWidgetCard` コンポーネントの作成。
- 既存の `WidgetCard` から内容を移行する準備。
- 既存のウィジェット（授乳、睡眠等）の内部コンポーネントのレイアウト調整。

## フェーズ 2: ダッシュボードのレイアウト刷新 (完了)
ダッシュボード全体を `HoneycombGrid` を使ったレイアウトに変更する。
- [x] `DashboardPage` のグリッドを `HoneycombGrid` に置き換える。
- [x] ウィジェットをハニカムタイル（2-1-2-1 等のパターン）に配置する。
- [x] ローディング画面（`DashboardSkeleton`）もハニカム構造に合わせる。

## フェーズ 3: 最終調整と検証 (完了)
視覚的なバランスと機能の最終確認を行う。
- [x] レスポンシブ表示（画面幅）に合わせた `size` と `gap` の微調整。
- [x] ダークモード対応と影（drop-shadow）の調整。
- [x] `sh scripts/verify_all.sh` による全チェックと動作確認。
